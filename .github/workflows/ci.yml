---
name: CI
on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/ci.yml
      - shard.yml
      - shard.lock
      - "**/*.cr"
      # Three shell scripts the jobs below actually execute, none of which matches
      # `**/*.cr`: without these lines a change to the spec partition or to the bench
      # type-check would merge without the job that runs it ever waking up.
      - scripts/spec_shard.sh
      - scripts/bench_check.sh
      - scripts/ameba_gate.sh
      # …and the lint gate's config: a relaxed or added rule must wake `lint-gate`.
      - .ameba.yml
      # Generated from shard.lock, and checked against it by `build-crystal`. The
      # lock is already listed above, which covers the drift that matters (a bump
      # landing without the regeneration); this line covers the other direction —
      # an edit to the generated file alone, which would otherwise merge with the
      # job that reads it never waking up.
      - nix/shards.nix
      # `docker/Dockerfile` is deliberately absent: nothing in this workflow
      # builds the image any more (see the note under `tests`), so listing it
      # would only wake the Crystal build and the spec matrix to test nothing
      # that changed.
  # Builds the PROSPECTIVE MERGE RESULT, not the PR branch. Without this, two PRs that
  # each pass CI alone and touch different hunks of one file can merge cleanly and leave
  # `main` broken — which is exactly what #371 + #372 did (#382): one changed a method's
  # arity, the other added a call site against the old signature. Inert until a merge
  # queue is enabled for `main` in the repository settings; harmless before then.
  merge_group:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      logLevel:
        description: manual run
        required: false
        default: ""
# Superseded PR runs are cancelled. Pushing a fixup thirty seconds after the first push
# used to leave the first workflow running to completion — eight jobs' worth of runners
# producing a verdict on a tree nobody will merge. Only `pull_request` runs collapse:
# the group for everything else is the run id, which is unique, so a push to `main` and
# a merge_group run each sit alone in their own group and never cancel or queue behind
# anything. (`cancel-in-progress: true` is harmless in a group of one.)
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: true
jobs:
  build-crystal:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: crystal-lang/install-crystal@v1
        with:
          crystal: 1.21.0
      - name: Check nix/shards.nix against shard.lock
        # The Nix flake builds from `nix/shards.nix`, which crystal2nix generates from
        # shard.lock (`just nix-shards`). Nothing forced the regeneration, and skipping
        # it fails silently in the worst way: the flake goes on linking the OLD
        # revisions and `nix build` stays green, so the packaged binary stops matching
        # the tree that produced it.
        #
        # No Nix job here to catch it — the flake pins its own compiler (nixpkgs is a
        # Crystal release behind) and building that costs minutes on a cold runner. So
        # this compares the two files directly instead: no Nix, no network, and it runs
        # before `shards install` because it reads nothing out of `lib/`.
        run: crystal run scripts/nix_shards_check.cr
      - name: Install native link dependencies
        # brotli/zstd are linked via `@[Link(pkg_config:)]` (proxy/codec), sqlite3
        # via the sqlite3 shard. The rest (openssl, yaml, gmp, pcre2, gc, zlib)
        # arrive as dependencies of the Crystal package itself.
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            pkg-config libbrotli-dev libzstd-dev libsqlite3-dev
      - name: Install dependencies
        # `--production` (= `--frozen --without-development`) rather than a bare
        # install. Two reasons, one of each kind:
        #
        # LIGHTER: the only development dependency is ameba — and only `lint-gate` runs
        # it, installing for itself — so `--without-development` skips a clone paid once
        # per job for a tool this job never invokes. `--frozen` costs nothing on top
        # and leaves shard.lock untouched, so this stays a read of the tree, not an edit.
        #
        # STRICTER: `--frozen` fails when shard.lock does not satisfy shard.yml, which
        # is the one dependency mistake a green build would otherwise hide — a shard
        # added or bumped in shard.yml with the lock left behind resolves fine here and
        # then installs something else on the next machine.
        run: shards install --production
      - name: Build
        # Deliberately NO `-Dpreview_mt`. Store, Fuzz::Engine, Miner::Engine and
        # Store::SafeRegexp each document that their plain ivars and unguarded
        # counters are safe *because* gori runs on the single-threaded fiber
        # scheduler. Enabling MT here would build a binary those files do not
        # describe. `--production` for the same reason as the step above — without it
        # `shards build` runs its own install first and puts ameba straight back.
        run: shards build --production
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: crystal-lang/install-crystal@v1
        with:
          # Deliberately NOT the build matrix. `crystal tool format` is the formatter
          # shipped WITH the compiler, so its output is a property of the version that
          # runs it, not of this repo — gating on two versions would ask the tree to be
          # a fixed point of two different formatters at once. Pin the one development
          # happens on; bump this line when that moves.
          crystal: 1.21.0
      # No `shards install`, and the paths are explicit rather than a bare `--check`:
      # the bare form walks the whole working directory, which after an install means
      # gating on `lib/` — third-party code this repo cannot fix, breaking the build on
      # a dependency bump for a formatting change nobody here made.
      - name: Check formatting
        run: crystal tool format --check src spec bench scripts
  benchmarks:
    # Type-checks every harness in `bench/`. Nothing ran them before: `format` above only
    # FORMATS bench/, and the other jobs build `src` and run `spec` — so a harness could rot
    # for months and the first sign would be someone reaching for it mid-investigation.
    # Seventeen of them had. AGENTS.md points at these for "measure, don't guess", and a
    # measurement tool that does not compile is worse than none: it turns a decision that
    # should be made on numbers into one made on a plausible story.
    #
    # `--no-codegen`, so this checks that they still describe the API they measure without
    # paying for optimised binaries — 26s for the whole directory on a cold cache. They are
    # NOT executed: several want a loopback origin or a seeded multi-GB database, and their
    # run times are measured in minutes.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: crystal-lang/install-crystal@v1
        with:
          # One version, for the same reason `format` pins one: the harnesses
          # compile against the same `src` that `build-crystal` already builds, so
          # an extra version here would re-test the compiler rather than this repo.
          crystal: 1.21.0
      - name: Install native link dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            pkg-config libbrotli-dev libzstd-dev libsqlite3-dev
      - name: Install dependencies
        # `--production`: see the note on the same step in `build-crystal`.
        run: shards install --production
      - name: Type-check bench harnesses
        # The same script `just benchmark-check` runs, so local and CI cannot drift apart
        # (and this job needs no `just` on the runner).
        run: scripts/bench_check.sh
  lint-gate:
    # ameba, as a DIFF gate: `scripts/ameba_gate.sh` counts findings per changed file at
    # the merge base and at HEAD, and fails only when a file GAINED some. The plain run is
    # deliberately still not a gate — the backlog is ~640 findings, most of them
    # Metrics/CyclomaticComplexity in the TUI that `.ameba.yml` chose to leave in place,
    # so a whole-tree gate is either always red (a check nobody reads) or green by
    # excluding that category (worse: new offences of the same kind would pass). The
    # per-file comparison neither pays the backlog down nor hides it; it only stops a
    # change from adding to it, and a new file starts from zero. `just lint-gate` runs
    # the same script locally.
    #
    # Pull requests only: the comparison needs a base, and on a push to main there is
    # none that says anything. Both sides are linted with the same compiled ameba and
    # this checkout's .ameba.yml (see the script), so a rule change on the branch is not
    # a regression. `shards install` WITHOUT `--production` here, alone among the jobs:
    # ameba is the development dependency, and since 1.7.0 the shard ships no
    # postinstall, so the script compiles `lib/ameba/bin/ameba.cr` itself.
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          # The whole history: the script diffs against the merge base with the target
          # branch, and a shallow clone has no such commit.
          fetch-depth: 0
      - uses: crystal-lang/install-crystal@v1
        with:
          crystal: 1.21.0
      - name: Install dependencies (with ameba)
        run: shards install
      - name: No changed file may gain ameba findings
        run: scripts/ameba_gate.sh "origin/${{ github.base_ref }}"
  tests:
    # Sharded, because `crystal spec` compiles the whole suite into ONE binary and runs
    # its ~10,900 examples in ONE single-threaded fiber scheduler — so this job was the
    # only thing anybody waited for: 8.5 of the workflow's 8.5 minutes, while `format`
    # finished in 18 seconds and the other two in under three minutes.
    #
    # Measured on this tree, the run splits roughly 1:5 between compiling (~35 s) and
    # executing (~370 s), and the executing half is barely CPU-bound at all — the suite
    # spends most of it parked on real sockets and deliberate sleeps that hold a
    # connection open so a stall stays distinguishable from a hangup. That is time four
    # runners can wait through at once, and the four-way split takes the job to well
    # under four minutes. It is NOT free: each shard pays the compile again, so the
    # workflow burns more runner-minutes than it did. Four is where that trade stops
    # paying — at six the compile plus job setup is already most of a shard's wall
    # clock, and the longest shard barely moves (43 s of examples against 49 s at four)
    # because the heaviest spec FILE cannot be split any finer than itself.
    #
    # Sharding by PROCESS rather than by threads inside one is deliberate: several
    # specs bind a fixed loopback port, and two of them in one process — or two
    # processes on one runner — would collide. One shard per runner keeps them apart.
    strategy:
      # A failing shard must not cancel the other three. The whole point of the split is
      # that one run tells you everything that is broken; fail-fast would turn it back
      # into "fix one, push, wait, find the next".
      fail-fast: false
      matrix:
        shard: [0, 1, 2, 3]
    name: tests (${{ matrix.shard }}/4)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: crystal-lang/install-crystal@v1
        with:
          crystal: 1.21.0
      - name: Install native link dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            pkg-config libbrotli-dev libzstd-dev libsqlite3-dev
      - name: Install dependencies
        # `--production`: see the note on the same step in `build-crystal`.
        run: shards install --production
      - name: Run tests
        # `-Wl,--no-export-dynamic` is a WORKAROUND for a linker bug, not a preference.
        #
        # Crystal links every binary with `-rdynamic`, which puts the whole program into
        # `.dynsym`. Past some size the produced spec binary's symbol table and its version
        # table stop agreeing, and the loader refuses it with a nonsense pairing:
        #
        #   undefined symbol: sqlite3_open_v2, version GLIBC_2.3
        #   undefined symbol: sqlite3_open_v2, version GLIBC_2.25   # same build, next run
        #   undefined symbol: environ, version Socket#read_timeout=<Time::Span>:Time::Span
        #
        # — a sqlite symbol tagged with a glibc version, a glibc symbol tagged with a Crystal
        # METHOD NAME, and the tag changing between builds of the same tree. Nothing is
        # actually missing: `ldd` is clean, the libraries are installed, and the same tree
        # runs green on macOS and links fine into `bin/gori` (the `build-crystal` job above
        # passes). It is the version index being read from a table that no longer lines up.
        #
        # Measured on the PR that first tripped it: `crystal spec` failed at load, `--no-debug`
        # failed the same way (so it is not debug info), and this flag ran the suite green.
        # The tree was 143 symbols and 0.19% larger than the `main` that passed — i.e. `main`
        # sits just under the cliff and the next change of any size would have gone over.
        # Scoped to this job because it is the only binary that crosses it.
        #
        # Nothing is lost: with debug info on (the default here) Crystal symbolizes backtraces
        # from DWARF, not from the dynamic symbol table, so spec failures still name their
        # frames. gori dlopen()s nothing and no linked C library calls back into a Crystal
        # symbol by name, so there is no consumer of the export either.
        #
        # DELETE THIS once the toolchain stops mis-emitting the table.
        #
        # `scripts/spec_shard.sh` decides WHICH files this runner compiles — the same
        # script `just test-shard` runs, so a shard that fails here can be reproduced
        # locally with one command. It partitions by walking the tree, never by a list
        # checked in here: a hardcoded split stops covering whatever spec file is added
        # next and nothing goes red to say so.
        run: crystal spec --link-flags "-Wl,--no-export-dynamic" $(scripts/spec_shard.sh ${{ matrix.shard }} 4)
# No `build-docker` job here. It used to build docker/Dockerfile for amd64 and
# arm64 on every PR, and those two builds — each compiling a static Crystal
# binary from source, the arm64 one on a native runner because QEMU is far too
# slow for it — dominated PR wall-clock while catching almost nothing: the
# Dockerfile changes far less often than the code it packages.
#
# It is not gated to `main` pushes either, because on a `main` push it would be
# pure duplicate work: `publish-ghcr.yml` already builds the same Dockerfile,
# for the same two platforms, on the same runners, on every push to `main` — it
# just pushes the result instead of throwing it away. A Dockerfile break is
# caught there, one merge late, before anything is released. If you ever want
# the image validated *before* a merge again, add `pull_request` to
# publish-ghcr.yml's build job with `push: false` rather than reviving a second
# copy of the build here.
