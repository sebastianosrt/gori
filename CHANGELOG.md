# Changelog

## Unreleased

- TUI: `/` filters the three list panes that had none — Discover FINDINGS (status / source / URL), Miner RESULTS (parameter / location / evidence) and the Authorize queue (method / host / path / verdict, so `bypass` and `pending` narrow a long queue to what needs eyes). One `RowFilter` (the OAST callbacks filter, lifted) backs all three: a `TextField` bar that takes a row of the pane only while it holds a query, `↵` keeps and `esc` clears, the card's count reads `7/40`, and the cursor stays on the row it was on across a narrowing. The filter is a lens, never a run scope — a hidden Authorize row still runs. Also: the Miner body used to answer every bare key as consumed, so `/`, and the detail pane's `x`/`y`, never reached the keymap from it.
- TUI: an opt-in **Tab numbers** layout pref (`layout.tab_numbers`, Preferences → Layout, off by default) paints `1:`…`9:` before the first nine tabs on the tab bar, the sub-tab strip's convention, so the `1-9 jump` the hint has always advertised has something on screen to point at. The number is the tab's visible position — what the digit keys have always answered to — so a bar scrolled to start at `5:History` still sends `5` there. Off, the bar is byte-identical to before.
- TUI: `y` copies a row on the nine lists that had no copy at all. Sitemap (the cursor row's `host/path`, or every marked row one per line), Discover findings (the URL), Diff rows (endpoint · verdict · what moved), the Issues LIST (`[severity] title (host)`, marks or cursor — the detail's `y` still copies the notes), Colormarker rules (the match filter, pasteable into a bar), Authorize requests (`METHOD host/path`), and the Project tab's env (`KEY=VALUE`), host-override (`ip host`, a hosts-file line) and scope-rule (`kind match-type pattern`) panes. All through the same `read_copy` seam and toast the other copies use (`copied Nb to clipboard`, with the clipboard-off / clipped caveat), and `nothing to copy` on an empty list.
- Sitemap: a double-click on a row's label folds or unfolds a folder, and opens a leaf's flow in the History detail (what `o` does). Expand/collapse answered only on the one-column ▾/▸ marker, where a double-click was a net no-op — the pair's first press toggled and the second toggled back — and the Target tab never forwarded the gesture to the tree at all. The marker column now reads a pair as one toggle, and a double-click off every row is delivered as an ordinary click.
- TUI: the `/` bars take the caret edits the cards already had. `⌃←`/`⌥←` and `⌃→`/`⌥→` move by word, Home/End jump to the line's ends, Delete removes the character under the caret and `⌥⌫` the word before it — on the History, Sitemap, Issues, Probe and Intercept filter bars, the sub-tab strip's filter, and the issue form's title. Each of these kept its own `@query`/caret pair beside `TextField` and answered the modified arrows with a one-character step and the other four keys with nothing; `LineEdit` is the one keymap they share now, with `TextField`'s word rule, so a hand moving between a bar and a card feels no seam. A delete re-runs the bar's filter the way `⌫` does; a motion does not.
- TUI: three idle costs off the render fiber. `Settings.reload_section` stats settings.json before it reads it — the three sections the TUI re-reads on every data_version poll (colour rules, saved views, Match & Replace) were three blocking `File.read`s up to four times a second under capture, and the cache only ever skipped the parse; a file whose mtime and size have not moved is not read. The Notes buffer reloads on that tick only while Notes is the active tab, like every other store-backed view (`on_enter` refreshes a clean buffer on the way back in). And the ui-state identity the 50 ms tick compares is a tuple, not a string interpolated and thrown away every tick.
- TUI: leaving a project stops the Authorize engine too, and an error is a notification, not a one-keypress toast. `Runner#stop_all_jobs` lists the seven job-owning controllers now; Authorize was the one without a `stop_all`, so its engine fiber — which owns its own sockets — kept replaying the batch under every identity after the operator was back at the picker, with the job pinned at `running` and no key that could stop it. A toast raised with `:error` is also written into the notification centre, so the `notify:` chip and the app.notifications card can bring back the one message an operator cannot afford to lose to the next keystroke. A Discover batch the store refused to persist used to be dropped in silence (the rows keep no bytes, so they could never be opened later); the drain says how many exchanges were lost and why. And the activity chip names `authorizing` / `saving results` / `loading results` instead of falling back to `jobs N`.
- Keys: one key, one meaning across the tabs a hand moves between. `d` no longer starts a Discover crawl on the Sitemap — on History, Issues and Probe, one ⇥ away, `d` deletes a row, and the same reflex here put requests on the wire; the crawl is a `space → d` menu row. `⇧X` is the wipe chord and nothing quieter: the Rewriter and Colormarker used it for "enable/disable everywhere", a toggle with no confirm, so the hand trained on the five tab wipes flipped every default rule off where it expected a card; those two are menu rows. `[` / `]` are the Global prev/next-tab chords everywhere now — inside an issue they raised and lowered its severity (hidden, unhinted), on the Rewriter they cycled its sections, and on Discover its runs; severity is a `space → +/-` row, the Rewriter's sections walk on `⇥`/`⇧⇥` (the focus ring, which the bracket keys had stood in for on the reading that the shell owned ⇥ — it does, through this hook), and the RUNS list's own ↑/↓ or two menu rows step Discover's runs. `i` on a read-only pane that sits beside an editor — the Repeater RESPONSE, the Fuzzer RESULTS/DETAIL, the Decoder OUTPUT, the JWT and Cookie DECODED/OUTPUT — used to fall through to the Global `intercept.toggle` and start holding every request on the proxy, with nothing tying that to the keystroke; it now says which pane takes `i` and where intercept is, and stops. `o` opens the row's flow on History, as it does on Sitemap, Probe, Issues, Discover and the Activity feed. The scope lens has one key, the Global `s`: the ⇧S twins on History, Sitemap and Probe were a second key for the same flip in the same tab, and the filter-bar chip now reads `s scope`. `^N` / `^W` open and close a Comparer sub-tab from the body, as on the Decoder, JWT and Cookie bodies. And History's hidden ↑/↓/j/k verbs are gated to History — `Scope::Body` is shared with Help and the Project settings pane, and they were the only Body verbs without the gate.
- TUI: confirm cards answer ↵ with the verb on their button, and the hint strips name keys the keymap actually knows. A card that asks something recoverable — *open in Repeater?*, *run the fuzz?*, *save the results?*, eleven of them — lit `cancel` and answered ↵ with "no" while its button said `open`/`run`/`save`, so the hand that had learnt ↵ = go had to learn `y` for these alone; a non-danger card lights its own verb now, a DANGER card (delete, quit) keeps cancel under ↵, and the hint is spelled from the buttons (`↵ run · y run · n/esc cancel`) rather than a generic `↵ select`. Every picker takes PgUp/PgDn/Home/End, one page being the rows it drew last frame — a 500-flow FlowPicker walked one row at a time. Four keys the OAST providers hint named (`a` add, `e` edit, `x` on/off, `d` delete), Discover's `p` pause and the Fuzzer RESULTS pane's `o` sort / `m` matched / `v` dist were raw `c == 'a'` arms in their controllers: no palette row, no space-menu row, and the hotkey editor offered the letters as free — a binding there would have been silently shadowed. They are chords now (`fuzz.sort`, `fuzz.matched`, `fuzz.dist` are new verbs), the arms are gone, and the hints read them off the keymap. The Sitemap hint no longer lists `space` twice.
- TUI: modals and prompts, made safe under the keys and pastes that reached past them. A confirm card raised over one of the four bottom prompts (`^G` go-to, `^F` find, sub-tab rename, the Repeater tag editor) could not be answered: the prompt claimed `y`, `n` and esc first, so `^D` with *Confirm before quit* on left a QUIT card on screen that nothing could close — the card takes the keys and the click now, and the prompt is still there when it goes. A paste over a modal is delivered key by key, so a pasted line break was ↵: on a one-line form that is COMMIT (pasting `/tmp/a.har⏎` submitted the import and typed what followed into the pane underneath), and on a confirm card a pasted `y` was an answer — `Overlay#takes_pasted?` lets text through and holds back ↵ on a one-line card, takes it as a newline in the four multi-line editors, and takes nothing at all on a confirm; the first held key is named once. A commit closure that opens a child modal in the parent's place (a picker off a form row, the Links card's add-picker) dropped the parent's pop-back with it; the child inherits it. Returning from the external editor (`^E`) put the caret on line 1 and cleared the undo stack on all four surfaces (Repeater request, Notes, Project description, Intercept editor) — `TextArea#replace_from_outside` keeps the caret and leaves the old text one `^Z` away. `⇥` typed a literal tab into every one-line field that handed printables to `TextField` (a project name, a library entry, an env prefix, the sub-tab rename) — `Key::Tab.to_char` is `\t` — and the field refuses it now so the card can move focus with it. `^Z` reaches the four modal editors (rewriter stub, discover headers, authorize identity, fuzz set values) that only the body editors answered; IME composition under the send-to picker no longer lands in the editor behind it; the Links card comes back on the row the add-picker was opened from; and the find-sub-tab picker opens on the active chip rather than row 0.
- TUI: the list-navigation contract every tab inherits, fixed in the base so no tab has to learn it twice. Re-entering a tab's body from the tab bar, a `Go to …` jump or a `[`/`]` cycle now lands on the pane the tab was last on (`TabController#focus_resume`) instead of its first — reading the Fuzzer RESULTS, `esc`, `↓`, and you were on the URL field; `⇥`/`⇧⇥` keep `focus_first`/`focus_last` as the ring's ends. PgUp/PgDn page by the rows the focused list DREW (`page_rows`), not a screenful of the whole body: Issues, Sitemap, Probe, Rewriter, Diff, Discover and the Intercept queue each skipped four to ten rows a press that had never been on screen — half a page with a preview open — and only History had corrected it; Intercept's page keys now walk the QUEUE, like every other list's, with the held-message preview reachable by wheel and by opening the editor. The wheel scrolls the pane UNDER THE POINTER on every multi-pane tab (`handle_wheel_at`, hit-tested with the same rect each tab's click uses) and never moves keyboard focus to do it — it used to scroll whichever pane held focus, and did nothing at all over the Fuzzer with TARGET focused; a notch or a right-click no longer wipes the toast being read. `⇥ preview` on History, Issues and Probe and `⇥ identity` on Authorize were promises only the mouse kept: the `key.tab?` arms sat in `handle_body_key`, which the Runner never reaches with `⇥` (the focus ring claims it first) — they are `pane_advance` now, and Authorize gains Home/End. A bare letter nothing binds in the current scope says so (`‹x› — nothing bound here · space menu · ? help`) instead of vanishing — typed text that missed its field used to disappear letter by letter except the Global breath keys, which fired (`s` flipped the scope lens, `c` stopped capture) with nothing tying the flip to the typing; modified chords and the named keys stay silent. Also: a filtered History list under capture no longer creeps the highlight down a row per matching flow (the reload moves the viewport with the row, as the unfiltered path did); the Issues preview writes its scroll clamp back so `↑` moves at once after an overshoot; and `j`/`k` walk the READ mode of the Decoder, JWT and Cookie inputs, Notes, the Issues notes and the Project description, where only the arrows did.
- MCP: a query naming a field QL does not implement is refused instead of searched as text. `ql.cr`'s `field_cond` free-texts an unknown field's WHOLE token on purpose, so `list_history{query:"methd:GET"}` ran a literal substring search over method/host/target and came back `[]` with `isError:false` — "this project has no such traffic" and "you spelled `method` wrong" were the same answer, on the one surface whose caller has no screen to notice it on. `strict:true` did not separate them either, and could not: an unknown field COMPILES, so `QL.analyze` files it under `applied`, which is the half `strict` never reports — and that is exactly the capability the exemption in `refuse_unknown_query_fields` rested on ("nor to MCP, whose `strict` argument already offers this"). `list_history`, `list_sitemap`, `probe_scan`, `diff_projects` and `authorize_start` now answer QUERY_SYNTAX naming the field and the spelling meant (``unknown query field `methd:` — did you mean `method:`?``), the way `gori run history` / `sitemap` / `probe` have since #884 and the way the MCP tools that SAVE a query (`create_view`, `create_color_rule`) already did; `lenient:true` is the escape hatch `--lenient` spells there, and `QL.field_shaped?` keeps a pasted URL, an authority (`acme.test:8443`) and a bare word out of it. It also names the one term an agent is most likely to copy off a screenshot: `v:` is the History filter bar's view CHIP, a label and not a term, and `query:"v:all"` used to come back as an empty page. `ql_explain` — the tool whose whole job is to predict this — reported `methd:GET` as an APPLIED term under `refused_by_query_tools:false`; it now carries `unknown_fields` with a `did_you_mean` per name, counts them into that verdict, and says the token is searched as text, which is why it matches nothing. Two loose ends beside it: `probe_scan` now DECLARES the `strict` argument it has always read (passing it was refused as an unknown argument, so the strict path was unreachable there), and `authorize_start` stops answering a misspelled field with "no captured flows matched — widen it", which sends a caller to loosen a query whose only problem is a typo.
- Intercept and extract rules: a condition naming a field a message in flight cannot answer is refused by name, on every surface that submits a complete one. `InterceptFilter::UNSUPPORTED_FIELDS` said "and the whole of that list" while holding `scope` alone, so the eleven other QL fields this backend does not implement — `size`, `reqsize`, `respsize`, `dur`, `stub`, `src`, the four `req.`/`resp.` splits and their aliases — fell through `field_symbol`'s else to FREE TEXT: MCP `intercept_set_filter{query:"dur:>500"}` was accepted, searched method/host/target for the literal string, and held nothing (negated, it held everything), which reads as intercept being broken. That is the exact degradation the file already refuses for `scope:`, and `FIELDS`' own comment already names the fields it is talking about ("`size:`/`respsize:`/`dur:` need an exchange that has FINISHED, and `stub:` needs a capture decision that has not been made"). The list is derived now — every QL field this backend does not implement, aliases resolved first so `res.body:` cannot free-text past the check — so adding a field to the backend retires its refusal in the same edit, and the one sentence the five refusing surfaces share (`intercept_set_filter`, `create_extract_rule`, `gori run intercept filter`, `Bindings#validate`, the TUI's extract-rule form and the intercept bar's note) says WHY per field rather than giving `scope:`'s reason for `dur:`.
- MCP: an OAST call that could not reach the provider is a retryable `NETWORK_ERROR`. `oast_start`, `oast_poll` and `oast_resume` returned their transport failures uncoded, and `Tools#classify` files every uncoded error under `INVALID_ARGUMENT` with `retryable:false` — so a refused connection or a DNS blip told the caller's error policy to fix its arguments and stop, for the one failure class that is transient by construction. `send_request` has always answered a transport failure with `NETWORK_ERROR` + `retryable` (`Tools.send_error_code`); these three agree with it now.
- Docker: the image no longer runs `gori` as PID 1. Linux drops any signal whose disposition is still the default when it is aimed at PID 1, and both shutdown paths were built on exactly that disposition — `App::SignalGuard` restores the terminal and then re-raises the signal at itself so the process dies FROM it and exits the conventional `128+signo`, and `gori mcp` installs no trap at all. In a container the re-raise was discarded, so `docker stop` on the TUI exited `1` instead of `143`, and on `gori mcp` the SIGTERM was dropped outright: the stop burned its whole timeout before SIGKILL (`exit 137`). `tini` is PID 1 now (`ENTRYPOINT ["/sbin/tini", "--", "gori"]`), gori is an ordinary child, and both paths exit `143` at once. `docker run --init` always did the same thing from the outside; baking it in means nobody has to know that. `gori run capture` was never affected — its traps wind down over a channel and never relied on the default disposition.
- Docker: `tzdata` ships in the runner, so `-e TZ=Asia/Seoul` works. Without `/usr/share/zoneinfo` `Time::Location.local` fell back to UTC without erroring, which made the variable a silent no-op and every timestamp UTC; 1.5 MB buys honest local time. The runner also gains `WORKDIR /data`, so a relative path handed to an export or a redirect lands in the mounted volume rather than the container's throwaway write layer, and `README.md`, `snap/` and `scripts/` join `docker/Dockerfile`'s ignore list — nothing under them runs during the build, and whatever is left in the context reaches `COPY . .`, where a single touch costs the full release recompile (a docs-only push to `main` rebuilds in 49s against roughly six minutes for a source change).
- History: the detail drill-in gets a footer strip under the text, and gori's own sentences move out of the pane. `sent by gori — repeater (tui) #4` and the `!` advisories were spliced into the REQUEST pane after the wire bytes, as if they were part of the message — a copy of the pane carried them, and a reader could take the note for a header. They now sit in a strip under the text, below a divider, on every pane (request, response, hex, frames, messages), and the strip leads with the exchange's facts the list had to abbreviate into five-cell columns: `200 · HTTP/1.1 · req 341B · res 152B · 37ms · application/json · 09-05 15:33:00` — status coloured like the list (`ERR`/`ABT`/`···` for a failed, aborted or pending flow, `STUB` in place of the protocol for a short-circuited one), wire sizes rather than the capture-capped blob, and `—` for size and latency until the response lands. The truncation and decode notes stay in the pane's trailer, since they describe the body above them. The strip yields to the text when the terminal is too short to keep three text rows under it, and the click/drag hit-test reads the same rect the render draws, so a click on the strip does not park the caret on the last text row.
- TUI: the Project tab's overview band drops its **Activity** row. It answered a question the tab is not the place to ask — the row read the project db's mtime at the moment `reload` ran, so it re-timed on every entry to the tab and said `34s ago` about nothing more than having just looked; a project's real activity is the ACTIVITY pane's event feed a sub-tab away, and its recency is what the project picker sorts by. `Created` stays, and the narrow tier's folded provenance line is now just `created Nh ago`. `Project#last_modified` is untouched — `gori run project list`, MCP `list_projects` and the picker still report it.
- Repeater and Fuzzer: a WebSocket captured over **HTTP/2** (RFC 8441 extended CONNECT) can now be re-opened and replayed. Seeding a session from one gives the WebSocket tab on all three surfaces instead of a plain HTTP one with a status line explaining what it was not carrying: gori dials h2 (ALPN, or h2c prior knowledge on a plaintext target), waits for the origin's `SETTINGS_ENABLE_CONNECT_PROTOCOL`, re-sends the capture's own extended CONNECT — `:method CONNECT`, `:protocol websocket`, and the `:path`/`:authority`/headers it recorded — and reads the `2xx`, not a `101`, as the socket opening. From there it is the same engine as the HTTP/1.1 path: one framed exchange, one set of capture caps, one transcript, the same masking and frame shapes, so a finding does not depend on which handshake opened the socket. Nothing is fabricated — the `X-Gori-Protocol: websocket` line in the request pane IS the `:protocol` pseudo-header and is folded back into it on send, which also stops a captured extended CONNECT replayed as an ordinary h2 request from carrying gori's own marker line to the origin as a header. Every way it fails is reported rather than showing as a clean run with an empty transcript: an origin that never advertises the setting is a refusal naming it (RFC 8441 §3 forbids sending the stream without it), a non-2xx is a refusal carrying the origin's head, and a `RST_STREAM`, `GOAWAY` or send window that never reopens keeps the frames already exchanged and adds the peer's own stated reason to the result's note. `--http2` on a WebSocket sweep is now refused only against an `Upgrade:` template, since HTTP/2 has no upgrade mechanism and an extended CONNECT is the h2 form; `^V`/`--http` has two stops on such a tab rather than three, because those bytes have no HTTP/1.1 form; and `--ws-keep-key` says it has nothing to keep instead of being ignored (§5.1 carries no `Sec-WebSocket-Key`). Per-message intercept and Match & Replace on messages stay HTTP/1.1-only — both would have to re-frame a DATA payload to a different length (#733).
- Decoder: twenty-seven defects a review of the tab found, across the TUI, the engine, its codecs, `gori run decoder` and the MCP `decode` tool. On the tab: entering it no longer re-runs the chain with hooks on and resets the OUTPUT pane — a persisted `exec:` conversion the project open had deliberately held ran its command the moment the tab was pressed, and a selection left in OUTPUT was thrown away by a round trip through History; `⇧↑`/`⇧↓` select in the INPUT editor (INS and READ) instead of moving, and never leave the pane mid-selection; `^S`/`^O` keep the focused pane rather than landing on INPUT; the CHAIN field scrolls with its caret, so a spec wider than the card is no longer typed blind past the border (a click into the scrolled field lands where it points); `^X` resets the OUTPUT caret and selection with the text it re-encodes, so `y` after a mode flip copies the hex line rather than "" or a fragment at the old offsets; a library save re-derives the ACTIVE conversion with its `exec:` step running, instead of replacing the decode on screen with "chain held", and an OUTPUT whose failure changed to a different failure redraws instead of keeping the old error under a PIPELINE that shows the new one; the PIPELINE previews are bounded and cached (three 16 MiB intermediates cost 1.5 s per frame, on every redraw) and a step's name is clamped inside the card; "copy all" and `y` agree on a failed chain, `^Y` from CHAIN copies the OUTPUT the footer promises, a rename persists at once, `^N`/`^W` drop a sub-tab filter that would hide the session they land on, and saving an empty chain is refused. In the library: a non-UTF-8 spec or name no longer raises out of `Decoder.run` (a Fuzzer marker over a binary region reached it) or out of `Settings.load`, whose blanket rescue then factory-reset every section after `decoder` over one hand-edited chain; an `exec:`-prefixed name is refused by `^S`, the settings parse and the registrar, since the chain grammar runs that token as a command and never reaches the chain it named (a separator inside a name is refused on the same three surfaces now, not only by `^S`); and a `^S`/`^X` whose write the settings file refused puts the library back instead of leaving a name that resolves in this process alone. In the codecs: `powershell-escape` doubles the four Unicode single quotes PowerShell's tokenizer also treats as delimiters; a gzip/zlib stream whose CRC-32/Adler-32 trailer disagrees with its bytes fails as corrupt instead of decoding as a clean Ok; an empty `gunzip` input is refused like its zlib and raw siblings; `base36-decode` folds only ASCII case (a KELVIN SIGN was a digit); and `xml-unescape` leaves `&#x+41;` / `&# 65;` literal. `gori run decoder list` measures its name column (two built-ins are 23 chars) and `--input` beside a 2nd positional is refused rather than silently winning; MCP `decode` takes the whitespace in `input_base64` that `base64-decode` tolerates and never returns more than its byte budget across a split multibyte char. Docs: the `sha256 | hex-encode` example double-encoded a digest, the CLI reference's `decoder` section described sub-tabs it no longer holds, and the guide named `raw-deflate`/`raw-inflate` by their aliases.
- TUI: `^F` search highlighting no longer freezes a pane on a token-dense line. The overdraw asked `String#index` for the next match by CHARACTER offset — which re-walks the line from its first byte on every call — and re-measured each match's column from the row start, so one long line with a match every other column (a minified body is exactly that) cost O(line²) per drawn row, per frame: 16k matches took 250 ms on ASCII and 38 seconds on CJK. The scan is byte-linear now, three forward-only cursors carry the character index, the byte offset and the column, and the loop stops once the band passes the pane's right edge — 0.04 ms and 0.7 ms for the same two inputs (#943).
- Sequencer: five defects an end-to-end audit found, most of them about how many requests a collection makes and how many tokens it admits to having. A run whose LATE samples miss extraction now tops itself up to the goal instead of ending below it — the dispatch loop stopped handing out work as soon as enough samples were merely in flight to *project* the goal, so a miss after that point lowered the projection with nobody left to react (measured 19/20 tokens at concurrency 1 and 35/40 at concurrency 5 against a 50%-extracting origin, on runs with 40 and 80 sends of budget still unspent, the shortfall growing with concurrency); it still lands exactly on the goal with no overshoot, and `max_sends` is still what bounds a descriptor that never matches. `^X` / `sequence_stop` now ends the retry chain too: a retry is a NEW request, and a stop mid-chain kept aiming the rest of them at the target and held the run open for `retries × retry-pause` per worker — 18 extra requests and 490ms in a measurement, up to 1000 requests and ~8 minutes per worker on MCP's ceilings, all after the stop had answered. An EMPTY extraction counts as a miss rather than a collected token: `Set-Cookie: SID=` (what an origin sends to *delete* the session once the replayed one goes stale) hands back `""`, which counted toward the goal and carried no error, while the analysis drops it — so a collection reported "500 collected" over a report reading "0 usable / 500 total · CRITICAL (no usable tokens)". And MCP `sequence_start` stops at twice the goal, like `gori run sequence`, when the caller names no `max_requests`: the server's 100,000-request ceiling was being passed as if it were the caller's own budget, which is exactly how the engine reads that field, so the goal-derived runaway guard never applied and the tool's #1 failure mode — a cookie name that matches nothing — put **100,000 requests** on the wire for a `count: 5` collection (measured; it now sends 10). Separately, the Sequencer tab refuses a `position` range the field does not spell (`100` with the `:B` forgotten, `a:b`, `40:8`) instead of silently reading it as `0:0` and running a real collection in which every sample missed — `gori run sequence --position` and MCP's `position` have always refused the same string (#945).
- Docs: add a capability matrix for protocol and entry-point boundaries, and correct claims about surface parity, WebSocket compression, HTTP/2 downgrades, and agent activity.
- Rewriter: seven defects a review of the tab found. The highlight follows the rule the operator acted on — `c` lands on the copy (it stayed on the original while the copy was appended off screen, so the `e` that usually follows re-opened the wrong rule), and an edit whose scope row re-homed the rule to the other block follows it there instead of staying on the row that slid into its index; a double-click on a rule or extract row opens it in the editor, as every other rule list does; the extract sub-tab's add/delete keys are the chords its strip names (`rewriter.add` / `rewriter.delete`) — the strip followed a rebind while the keys stayed `a`/`d`, and the Colormarker's custom-colours pane had the same gap — and both empty-state lines name the effective chord too; PgUp/PgDn/Home/End move the rule, extract and binding lists; the preview INPUT lets go of the focus when the terminal shrinks the preview pair out of the layout, instead of typing into a pane that is no longer drawn; and the rule form walks past the rows a header op ignores (match, part, and the value row of `remove header`, whose ↵ on the header name now saves) instead of parking the caret on `n/a` rows where ←/→ re-ran the flow scan for nothing (#940).
- Notes: a paste lands as **one edit**, and an idle key no longer dirties the note. Notes was the one multi-line editor still taking a bracketed paste keystroke by keystroke — the Repeater and Fuzzer opted into the bulk path when it landed (`TabController#accepts_bulk_paste?`), and Notes is the tab a captured response, a tool's output or a whole writeup gets pasted *into* — so a 200-line paste paid 200 undo snapshots, 200 highlight rebuilds and 200 frames (quadratic in the paste; the Repeater measured minutes on a 244 KB body), and `^Z` then took it back one character at a time. In INSERT the note now splices the clipboard in as one undo step; READ still refuses a paste rather than running it as commands, exactly as before. Separately, `⌫` at the buffer start, `⌦` at its end and `^Z` on an empty undo stack marked the note dirty while changing nothing, and `dirty?` is not a cosmetic flag there: it is the lock that holds off a peer's reload (`locked?`) and what makes the next esc or sub-tab switch rewrite the whole document — so a stray `^Z` in a clean note silenced every peer refresh until something else saved. The three now dirty only on a real buffer change — and so do the two editors that were still missing the same gate: the Fuzzer template's `⌦`, which re-persisted a template nobody had changed, and the Project description's `⌫`/`^Z`, where the flag is what makes `reload` refuse to re-seed the buffer from the store, so an idle keystroke over clean prose muted every later refresh of that pane including a peer's write. Found by stating the rule once over every multi-line editor in the TUI rather than fixing the fifth one by hand.
- History: eleven defects a review of the tab found. The detail's send verbs (`^R`, `⇧F`, `⇧I`, mine, sequence, active scan) act on the flow on screen again, not on the marks or a follow-moved cursor, since they close the drill-in before resolving their target; a row click while the QL bar is editing selects the row under the pointer instead of the one below it, and keeps the row across the reload the click applies; a clear that rolled back on a busy project leaves the marks and the open detail alone, and `count` no longer reads as 0 under a busy store — the TUI says so instead of swallowing `⇧X`, and `gori run history clear` / MCP `clear_history` refuse instead of reporting "0 flows"; reveal (`b`) over a binary or gRPC pane walks the placeholder on screen rather than invisible raw lines, and paints the ⇧-selection it copies; the Req/Res preview's scroll is bounded so `↑` moves at once after an overshoot; PgUp/PgDn step by the rows the list or detail actually drew; a rebound `y` in the detail body is no longer shadowed by a hard-coded copy; and the `gori run history` text listing keeps a space after a 6-digit id (#935).
- TUI: a **Mouse** settings section, and a drag that can copy what it selected. `Ctrl-,` → **Editor & Keys** → **Mouse** (or `settings:mouse` in the palette) now holds the pointer prefs: the on/off toggle, which spent its life as a lone row under **Editor** — the one heading nobody looking for pointer behaviour opens — and a new **Drag release** mode. `select only` is what gori has always done: the band stays highlighted and waits for the copy key (`y` in READ, `^Y` while typing). `select + copy` also puts it on the clipboard the moment you let go, the way a terminal's own primary selection does — the gesture gori took away when it claimed the mouse in the first place. It fires only on a drag that actually built a selection, so a plain click still copies nothing (without that guard every click would have copied the WHOLE pane, which is what the copy verb does with nothing selected), and not at all while a modal is open (a modal that takes its own drags extends its own field, which `read_copy` cannot see — the tab underneath's stale selection is what it would have copied). It goes through the same `read_copy` seam the keyboard uses, so what "copy" means on each tab and the toast that confirms it cannot drift between the two. A drag that ends on its own press column (straight down the target URL row) is no selection, so it copies nothing rather than the whole URL; and the JWT ENCODE panes and the Cookie FORGE payload copy on release like every other pane. The persisted key is unchanged (`mouse`), plus a new `mouse_drag`.
- Projects: a project's "last active" time is no longer frozen at the moment it was created. gori runs SQLite in WAL mode, so a live session's writes land in `<db>-wal` and the main file's mtime does not move until a checkpoint — which is when the last connection closes — and `Project#last_modified` read only the main file. The Project tab's overview band drew `Activity: 16m ago` above `Created: 7m ago`, activity older than creation, for a project that was capturing at that second; and every peer reading a LIVE project (`gori run project list`, MCP `list_projects`, a second TUI's picker) got the creation time as its "last active". It now takes the newer of the two mtimes — which also changes which project gori DEFAULTS to (`registry.list.first`, and `gori run`'s no-flag project resolution with it): a project being written to right now sorts first, where before it sorted as though it had not been touched since it was made. `Store.captured_flows` already guarded the opposite direction — a read-only census closes the last connection, that checkpoints, and the project would be stamped "just active", so it puts the mtime back — and it now puts back the ACTIVITY time, on both files: a checkpoint deletes the WAL that carried it, and a stale WAL SQLite cannot replay is reset in place with a fresh stamp, so restoring only the db file's own older mtime made `gori run project list` sort a project by one time and print another. The **Activity** row itself has since been dropped from that band; the fix is what every peer reads. Same fix in the same band for **DB Size**, which read `4.0 KB` for a project holding 3.1 MB of capture: the tab now reports the db file plus its write-ahead log. MCP and `gori run project`'s `db_size` field is deliberately unchanged — it names the database file, and `disk_size` beside it already counts everything.
- TUI: the eight Copy verbs are rebindable, and `^Y` is pinned. `Hotkeys.rebindable?` refused any verb declaring more than one chord — written for navigation aliases like `body.open` (`↵`/`→`/`l`), where a single-chord rebind would collapse the set — and it swept up every `y` + `^Y` Copy pair with them. The result was visible and unexplained: Settings → Hotkeys showed `Copy  y` under FLOW DETAIL, MINER, SEQUENCER, PROBEDETAIL and COMPARER, `Copy  ctrl-y` under INTERCEPT, and under REPEATER and JWT no Copy row at all — half the app's copy keys movable, half not, with nothing on screen saying which. Repeater, Fuzzer, Notes, Decoder, JWT, Cookie, Rewriter and the Issues detail now offer theirs. A rebind moves the READ letter and leaves `Ctrl-Y` where it is (`Verb::Keymap.pinned_chords`), including through an explicit unbind: `^Y` is the copy key in INS, where a bare `y` is a literal character that would replace the selection, and it is the same chord on every tab — unbinding `y` says something about READ mode, not about that.
- TUI: `Ctrl` + a letter no longer fires the Rewriter's and Colormarker's bare-letter actions. `Event::Key#char` falls back to `key.to_char` and a terminal sends Ctrl+A as the letter plus a modifier, so `c == 'a'` matched the Ctrl form too — `^A` opened ADD EXTRACT RULE / ADD CUSTOM COLOUR, `^E` ran the edit action, `^X` toggled a rule, `^K` walked the list. Ten of the twelve body controllers already opened with `return false if ev.ctrl? || ev.alt?`; these two did not. It cost twice over: `^E` is in `Hotkeys::CLAIMED_CTRL_LETTERS` (the global "open in `$EDITOR`"), whose whole contract is that a controller may hardcode a Ctrl guard only for a key named there, and `^A`/`^X` are bindable in the hotkey editor, so a user's own binding was silently shadowed on those panes. The Rewriter guards at its two list handlers rather than at the tab's door, because the preview INPUT beside them is a real text editor and still needs `^Z` and `⌥⌫`. The Issues notes pane's `^X` (which ran select-line) is closed the same way. The same trap through `Event::Key#key` rather than `#char` is closed too: the parser decodes 0x01..0x1A as the letter plus Ctrl, so a `key.lower_k?` arm is true for `^K` (and an ⌥ event carries the letter as well) — the Comparer's whole `hjkl` ladder had no ctrl/alt defer at all, and the Fuzzer's, Miner's, Sequencer's and Discover's empty-state handlers ran BEFORE their tab's, so `^K`/`^L` and every ⌥ letter walked those panes instead of reaching the binding. `^H`/`^J` were never reachable (`Verb::Reserved` refuses them for the reason a terminal sends them as Backspace/Enter). `TabController#nav_up?` and its three siblings are now the one home for “the arrow, or its vim twin pressed bare”.
- TUI: six more places where a rebound key was still named by its default. Four empty-state cards — the Project tab's SCOPE, DNS MAP, VARIABLES and ACTIVITY panes — printed their `a`/`s` chip as a literal, so after rebinding "Add env var" the status strip beside the card said `n add` while the card, the only thing on screen offering to teach the key, still said `a`. Two body hints (`x line` on the OAST callback detail and the Rewriter preview output) and, in Help, the flow-detail, Issues-notes, Notes, Decoder and Rewriter rows did the same for their `x`/`y`. What stays literal now says why: the Project description row's `x`/`y` are dispatched raw and their verbs carry no bare chord to read. Then the short-pane tier of every empty-state card (the medium fallback was written as literals, so shrinking the pane made the rebound key vanish from the card that had just shown it), the Rewriter extract strip's and the Colormarker custom-colours strip's `a add · d delete`, and Help's Fuzzer, JWT and Cookie `y · ^Y` rows, whose READ letter now follows a rebind while `^Y` stays pinned.
- Export: a captured request line gori cannot FRAME as `METHOD SP request-target SP HTTP-version` no longer becomes a runnable command asking for the wrong resource. `Export::Curl.parse_request_line` takes `parts[1]` as the target, so on a line with an unencoded space, a doubled space, or a tab where the delimiter belongs, that token is not the target at all — and every "copy as <tool>" surface built a URL out of it. Measured against a raw listener: `GET http://h/echo?a=b c&d=e HTTP/1.1` came back as `curl 'http://h/echo?a=b'` (query truncated), `GET  http://h/admin HTTP/1.1` as `curl 'http://h'` (path gone), and a tab-delimited line as `curl 'http://h/HTTP/1.1'` — a resource nobody requested. python / fetch / Go / httpie / CSRF agreed with curl, `--format json|har|raw` kept the bytes, and the runnable half was the wrong one. `FlowMapper` already refuses to STORE a derived target for these lines ("History would render a deceptively-plausible-but-wrong URL"); the export surface now refuses too — curl hands back a `# no command:` comment naming which framing failed, the other five drop out, and the TUI's copy menu drops its URL, language and wscat rows while keeping the byte-exact ones and the cURL row's note — also when the line resolves to no URL at all, where the note used to drop out with the rows it explained. A 3-token line whose TARGET holds a tab still exports: that tab is the operator's payload (DESIGN.md §7), not a delimiter.
- CLI: `gori run mine`, `gori run sequence` and `gori run repeater minimize` say when a `--slot` overlay went out with `$NAME` unbound. All three accept `--slot` and all three print a run summary, which is exactly the condition `Env.take_unbound_overlay`'s doc reserves the drain for ("a log line the operator is not tailing is not a report") — they got only the raw `Log.warn`, written BEFORE the results. So a mine reported "0 found" for parameters only a session reaches, a sequencer rated the tokens an ANONYMOUS client is issued, and a minimize calibrated against an anonymous baseline and `--apply`'d the result. The drift guard that was supposed to catch this listed three files by hand and was stale in both directions; it now DERIVES the population from "every `gori run` verb that accepts `--slot`".
- JWT: `gori run jwt --attacks` names the key it already recovered. The weak-secret family re-signs under each dictionary key, and for a token signed with one of them the generated signature was byte-equal to the token's own — gori held the proof and still printed "verifies if the server's HMAC key is …", an invitation to go send a request and find out. The matching row is now marked `✓ SECRET FOUND` with the `--encode --secret` line to forge with, and `verified` rides on every row of `--format json` and MCP `jwt_attacks` so a consumer can select on it. It is a real verification — over the ORIGINAL header/payload segments, under the token's DECLARED alg — so a re-serialized header cannot hide a match and an HMAC coincidence over an RS256 signature cannot fake one.
- TUI: a rebound shortcut now shows up everywhere the key is named. The command palette and Help's single-verb rows already read the effective keymap, but forty-odd status-bar hint strips (Discover, OAST, Diff, Sitemap, Sequencer, Miner, Project, Decoder, JWT, Cookie, Authorize, Probe, Comparer, Rewriter, Colormarker), the empty-state cards' chord chips, the tab bar's `c capture · i intercept`, and Help's composite rows (`^R · ^X` run/stop, the OTHER TABS summaries) were hand-written literals — so after binding `run` to `^S` the strip still said `^R run`. A hint now spells a chord as `{verb.id}` and `Hotkeys.expand` resolves it, and the Comparer strip's `⇧n` for prev-change reads its own verb instead of assuming shift + whatever next-change is bound to. What stays literal is not a verb: the claimed `^P`/`^N`/`^W` family, `esc`/`↵`/arrows, and a pane-local `x`.
- TUI: sub-tab strips multi-select. `t` marks the chip under you and steps right, `⇧T` marks every chip the `/` filter shows, `esc` clears; `^W`, `^R` (Repeater), `space ▸ d` and `space ▸ t` then act on every marked sub-tab behind one confirm, and the strip's space menu says so (`SPACE · 3 MARKED`, `Close 3 sub-tabs`). The Repeater strip's bare `t` used to open the tag prompt — that is `space ▸ t` now, so `t` means mark on every list and strip in gori. One mark is a batch of one: `t` steps right after marking, so a single mark and the cursor usually sit on different chips, and the gesture acts on the mark rather than the chip under the cursor. The marks travel through the confirm as the sessions themselves, so a peer reordering or deleting a session while the dialog is up cannot make `^W` close a different one (#683).
- Repeater: `--verbatim` / `verbatim:true` now stops a DECLARED session binding too. The flag promises "no `$VAR` expansion" and delivered it for project env vars, but an extract rule's or session slot's `$NAME` was still substituted one layer further down, at the send seam — so `gori run repeater send --verbatim`, MCP `send_request{raw|repeater_id, verbatim:true}` and a verbatim WebSocket handshake all sent `GET /api?SECRETTOKEN123=1` where the stored bytes read `GET /api?$TOKEN=1`: a request nobody wrote, with a live credential in a position the operator chose as a payload. The scope gate reads those same literal bytes, so a path-scoped rule now judges the request that actually goes out. The active session slot's header overlay still applies — it answers *as whom*, not *which bytes*; under verbatim nothing interprets the `$` grammar at all, so the `$$name` escape is not consumed either (#910).
- Repeater: a captured WebSocket tab's handshake is replayed byte-exact. `Sender#send_ws` carried its own copy of the send seam's two passes and that copy expanded `$NAME` unconditionally, so a WS tab seeded from a capture was still substituting where the same tab's HTTP send had stopped. The TUI now names the withheld tokens on the WS status line, the way the HTTP one already did (#910).
- Repeater: the Sandbox gate is taken on the bytes that go on the wire. `send_wire`/`send_ws`/`send_group` re-ran the binding pass over bytes it had already run, and that pass is not idempotent — it also consumes `$$` — so a stored `GET /api?$$TOKEN=1` was sent as `/api?$TOKEN=1` while the scope was asked about `/api?SECRETTOKEN123=1`. A send-group now makes one pass per request instead of two (#910).
- Repeater WebSocket: a server frame larger than the 16 MiB per-frame cap is recorded as a truncation with a marker in the transcript, instead of read as EOF and reported as "the connection ended" — the frame and the fact that the origin answered both used to vanish. And an origin that trickles a frame's payload one byte at a time can no longer pin the send indefinitely: the read is now bounded by the drain deadline.
- Repeater over HTTP/2: a request header NAME carrying a non-UTF-8 byte goes out byte-exact. The default field-name lowercasing ran a full Unicode `downcase`, which rewrote such a byte to U+FFFD (P7); only ASCII letters are folded now.
- Repeater (TUI): several dogfood defects. An arrow key in the response pane no longer crashes the app when a shorter response has replaced a longer one (a stale caret read past the new end). A request save the store rolled back is left dirty and retried, so the next reconcile can't repaint the old row over your edit; the WebSocket `^R` persists the handshake and frames before the response lands; and `^W` on a single sub-tab closes the session you pointed at even if a peer reordered the strip while the confirm was up. A duplicated grpc-web-text tab now sends the same framing its source does.
- Repeater (CLI/MCP): `gori run repeater send` writes its stored response and `--record-history` flow to the project it read, not to whichever project happened to be most-recently-active when the send finished. `gori run repeater minimize` accepts `--insecure-upstream` (the spelling every sibling uses). `create_repeater` with an extreme `position` no longer crashes the next new tab with an integer overflow, and `send_request` answers a clean error for an over-long port instead of an internal one.
- Probe: four more passive checks — a suspected subdomain takeover (the provider's unclaimed-resource page behind a customer hostname), a password submitted or a login form served over http://, a Set-Cookie or reflected CORS origin a shared cache may store, and an internal host named in a response header. The cache checks combine repeated fields without treating quoted extension text as directives, cacheable API findings require a real JSON media type, cleartext JSON credentials no longer stop at an earlier UI-state member, and security headers follow Referrer-Policy fallback order while rejecting malformed or duplicate HSTS max-age directives.
- Probe: the body-differential active checks no longer read a truncated response as a finding. A response the origin cut short, or that hit the capture ceiling, comes back successful but incomplete, and its short body was compared as if whole — so a flake landing on the backslash-powered `\` leg (and not its `\\` twin), a URL-rewrite probe cut off below its root control, or an error-based-SQLi baseline ending before an always-present database error each fired a false positive. All three now decline on an incomplete leg or baseline, the way the Next.js server-action check already did.
- Probe: a new active check, `cmd_injection_oast`, confirms blind OS command injection out of band. It appends a shell-breakout OAST payload to a command/diagnostic parameter (`cmd`, `ping`, `host`, …) and raises the finding when the server's shell calls the project's OAST listener back — same `needs OAST` gate as blind SSRF.
- MCP: `delete_probe_rule` refuses a delete the store rolled back instead of reporting `{"deleted":1}` while the rule keeps firing (and logging success into Activity); `create_probe_rule` and `create_oast_provider` answer retryable `PROJECT_BUSY` on a rolled-back insert, matching every other create on this surface (#918).
- TUI: the Fuzzer's `^N` line no longer tells you to type into a field that is in READ. `load_blank` opens the TARGET in READ mode, so the toast's “type the target URL” named a gesture that edited nothing — and because the field holds a URL, its digits fired the global 1-9 tab jump and walked the operator off the session they had just made. It names `i/↵` now, the way `repeater_new` already said “edit” (#912).
- TUI: a confirmation that cannot be drawn can no longer be committed. `ConfirmDialog` demanded its full height and drew NOTHING below it while `handle_key` went on answering, so on an 80×12 terminal `⇧X Clear history` armed a modal that showed no heading, no count and no “This can't be undone” — and `y` still deleted every flow in the project. The card now degrades instead, keeping its heading and buttons down to four rows and clipping the message with an ellipsis, and below that it refuses `y`/↵ and stays up so a resize can rescue it (`n`/esc still cancel, because cancelling an unreadable confirm costs a keystroke and committing one costs the data). This is the rule ProjectPicker already applied at its own open site, moved to where a resize after arming can also reach it, and it covers every confirm the Runner raises (#912).
- TUI: three lists stopped painting outside themselves on a small terminal. History's STA cell is the one column in its right-anchored cluster that was not gated on spare room, so at 40 columns with a `strip` colour rule armed it drew from the fixed `host_x` straight over the frame's hairline — `2┃0` on every row, the scroll gauge painted through the middle of a 200. The OAST CALLBACKS table drew its header labels with no width at all and floored the row's DESTINATION cell at six columns whatever was left, so the right-anchored PROVIDER started left of the fixed runs and the three overwrote each other (`PROVIDERTINATION` at 70 columns, the provider eating the source IP and the header leaving the card at 52). And the Notes link preview, carved off the editor's last row, landed on the filter bar above it when there was no row to spend (`/linkser[repeater] XSS PoC (+2)`) (#912). Four more of the same shape, this time found by sweeping every tab body across every size `Layout.usable?` admits rather than one pane at a time: the shared `├───┤` section divider (History, Issues, the Sitemap, Probe, Discover, the Comparer and the empty-state cards all count their seam row down from their own top and none checked the answer against the pane), History's “no flows match the … view” message and its hint, the Probe list's `SEV CAT TITLE` header, and the Project tab's whole OVERVIEW band — each landed on the shell's status row, over the key hints, on the 8-row terminal that is the supported floor. History's message also ran two columns past the card's right border into the terminal's own margin at 40 columns, because it passed no width and `Screen#text` then falls back to the whole screen.
- Authorize: a request whose **baseline was itself refused** (4xx/5xx) no longer reports as a BYPASS on any surface — nobody was served the resource, so a matching denial is not a finding, and a captured flow that 403s, a plain 404 or an expired baseline cookie used to paint a whole run red. Those comparisons read `review` now, and each surface says why (`⚠ the baseline itself answered 403`, MCP `baseline_denied_count`, the TUI run summary).
- Session slots: `gori run session add`/`edit`/`from-flow` and MCP `create_session_slot`/`update_session_slot` refuse a name that collides case-insensitively with an existing slot, which is how Authorize compares them — creating `admin` beside `Admin` left every authorize run in the project refusing to start.
- CLI: `gori run sequence --tokens` refuses `--max-requests`, `--rate`, `--throttle`, `--timeout` and `--no-keep-alive` instead of accepting and ignoring them; a pasted list sends nothing, so none of them could ever apply.
- Export: the code a "Copy as" row hands over now RUNS, and all five languages ask for the same resource. curl gets `--globoff`, so a URL holding `[]`/`{}` (a JSON:API `fields[item]=…` among them) is fetched literally instead of dying with `curl: (3) bad range` or fanning out into one request per glob expansion; httpie drops a field whose bytes are not valid UTF-8 and names it, instead of a command that aborts with `UnicodeEncodeError` having sent nothing; a `fetch()` for a captured GET/HEAD-with-a-body says the body cannot ride rather than throwing; and a URL's non-ASCII path and query are percent-encoded for curl, Go and httpie too — while the HOST is now left raw, because `requests` reads a percent-encoded one as a name no resolver answers.
- Import: a HAR entry stating `Content-Length: 0` with no `postData` keeps that header. `Export::Har` writes exactly that pair for a captured request with an empty entity, and the importer's re-emit was gated on there being a body — so gori's own HAR round trip turned a POST framed `Content-Length: 0` into one carrying no framing header at all.
- Proxy: a flow keeps the response head gori refused to relay (`Content-Length` and `Transfer-Encoding` together, a `1xx` that declared a body) instead of recording only the refusal, and marks a start-line that is not a status line — the shape a body over-running its `Content-Length` leaves for the next request on a reused connection — which History used to show as a clean `200`.
- CLI: `gori run show --format raw` says on STDERR when the bytes it printed stop at the capture cap, as `text`, `json` and `har` already did.
- Discover: character references in a crawled link are resolved before the URL is built, so `href="/list?page=2&amp;sort=name"` is crawled as `?page=2&sort=name` instead of sending the origin a parameter named `amp;sort`; the numeric spellings were worse, `&#38;` reaching URL resolution as a live `#` and cutting `?id=6&#38;view=full` down to `?id=6`. Only `;`-terminated references and only the five names XML predefines are decoded, so a legacy `?a=1&amp=2` still goes out as written.
- MCP: `send_request` refuses a stored source (`flow_id`/`repeater_id`) passed together with `url`/`method`/`headers`/`body`/`raw`/`h2_fields` instead of replaying the stored request and naming the dropped overrides afterwards, so an unintended resend can no longer happen before the warning arrives; `mine_start` and `sequence_start` likewise refuse two template sources. `verbatim` now reaches a `repeater_id` replay, matching `gori run repeater send --verbatim` (#906).
- Fuzzer: **Auto Content-Length now means what it means in the Repeater** — it ADDS the header to a template that carries a body and declares no length, instead of only recomputing one that is already there. The knob had only ever rewritten a declared header, so a request with a body and no `Content-Length` went out with the body on the wire and nothing framing it, and an HTTP/1.1 origin read a **zero-length body**: a request body has no close-delimited form, so `Connection: close` cannot delimit one. Every payload of the sweep was then scored against a request the origin never read a body from, under statuses that looked like a clean run. The same request replayed one tab over worked, because the Repeater's `^L` has added the header for as long as it has existed (`FlowRequest.resync_content_length`, `add_if_missing: true`) — which is exactly what kept this invisible: a template seeded from a repeater session, or hand-authored without the header, behaved differently in the two tabs that share it. Turning it off is still spelled by turning that same switch off (`--verbatim`, MCP `update_content_length: false`, the ADVANCED card's **Auto Content-Length**), because a length that disagrees with the body — or no length at all — is a real desync test. **And when the switch leaves a body unframed, gori says so before the first send** rather than letting the run go quiet: `Plan#unframed_body?` reports a body carrying neither `Content-Length` nor a chunked `Transfer-Encoding` that nothing will frame, as a CLI warning naming the flag and on the Fuzzer tab's run-start line, where it retracts itself the moment the template is edited to declare one. It is a note and not a refusal, for the reason `ws_ignored_knobs` is: an unframed request is a legitimate thing to send on purpose — what is not legitimate is sending one by accident and being told nothing. **Adding is held to a higher bar than recomputing**, so nothing is invented beside a `Transfer-Encoding` header of any shape: `ContentLength.chunked?` answers the RFC 7230 §3.3.1 framing question (is the FINAL coding chunked), which reads `chunked, gzip` as not chunked and looks only at the first TE line — right for leaving an existing `Content-Length` alone, and wrong for inventing one, since the pair gori would then manufacture on every request of a sweep is the canonical CL+TE smuggling primitive the operator never wrote. `FlowRequest.resync_content_length` has always refused on any `transfer-encoding:` line; the fuzz module now states the same rule. `rewrites_content_length?` was narrowed to the rewrite it names while this landed: computed off the whole baseline rendering, it would have reported an ADDED header as "your declared Content-Length disagrees with its own body" — a sentence about a header the operator never wrote — and it had been folding a gRPC prefix reframe into that same claim (#905)
- TUI: `⇧X` clears the **Issues** tab too. #899 rolled the clear-all chord out across the four verbs that empty a tab — History's flows, Probe's findings, the Authorize queue, the ACTIVITY feed — and the Issues list, which holds the hand-written writeups an engagement produces, had no clear verb at all: the key an operator learns as "this clears the tab" did nothing there, on the one tab whose contents cannot be re-captured. It now deletes every issue in the project with its notes, CVSS score and evidence links, behind the same confirm the other four ask, and the prompt names the total. That total is read from the STORE, never from the visible rows: `IssuesView#empty?` answers for the FILTERED list, so gating the wipe on it would have told an operator holding forty issues that there was nothing to clear the moment their filter matched none of them. It is not mark-aware either — `d` is the verb that acts on a marked set — so the marks state's body hint reads `⇧X clear ALL` where the other states read `clear`, and every list state names the key, since the scope does not change with the preview pane. An empty project gets a toast rather than a dialog, the way Probe's and ACTIVITY's answer theirs. The Help sheet's Issues row gains the chord, and `docs/guide/hotkeys`, `scanning`, `authorize` and `proxy` name five members where they named four. The family is now enumerated from the `:wipe` band in the registry rather than a list of ids restated in a spec — the four-id literal claimed a fifth member picking its own letter would fail there, and could not, which is exactly how this gap stayed invisible (#903)

- Fuzzer / confirmations: seven defects a dogfood of #897 turned up, all of them cases where a surface said something that was not true. **A confirmation no longer loses its second clause.** `ConfirmDialog` caps its card at 60 columns and handed each line straight to a renderer that CLIPS at 54, so all three of #897's two-line messages ended in `…` — and in each the clipped half was the consequence: `CLOSE FUZZER` drew "Its template/config, private temporary spool, and eve…" and dropped "ry saved run are deleted.", which is the only warning an operator gets that closing a fuzz sub-tab destroys every run `⇧S` promoted into the project. The card now wraps (in terminal columns, so a CJK message breaks where it is drawn) and grows to hold what it wraps, which fixes the class rather than the three sentences. **A restored run keeps knowing which rows the display dropped.** A row past the window's 64 MiB ceiling is kept as bounded metrics — its payload cut to a 64 KiB preview ending in `… [display truncated]` — and the restore handed the view that window's ROWS rather than the window, so an already-projected row (now small) was re-admitted as a full one: the request pane rebuilt a request from the preview, and `Send to Repeater` seeded those bytes as the request the archive holds in full. **The response detail pane names the bounded display** instead of reporting a run with `keep_bodies: all` as having retained nothing. **`⇧S save` appears in the footer only while the verb would fire** — it is gated on a finished, non-empty, unsaved run, and an unavailable verb is never dispatched, so the key did nothing and said nothing while the hint kept promising it. **A run whose spool failed now says so on the results line** — `archive_failed?` shipped with no reader at all, so the one announcement (the run-start status) was overwritten by the completion toast and a sweep that can never be saved then looked exactly like one that can. **`gori run fuzz list` labels a legacy snapshot `[LEGACY]`**, off the same record method the TUI picker reads, instead of asserting `[H1]` from columns a pre-V24 row never carried — on the very command the picker's refusal points the operator at. And **`gori run fuzz list/show --format json` carries `created_at_iso` / `finished_at_iso`**, so its output can be correlated with `list_fuzz_runs` as strings, the gap History's row emitter already closed (#897, #901)
- CLI: `gori run` refuses a stray positional argument instead of dropping it. `OptionParser`'s default handler is silent, so seventeen subcommands discarded any leftover word and still reported success for work they had not done — `gori run project scope add -p api.example.com note` added the rule and said nothing about `note`, `gori run rewriter add -f a -v b STRAY` printed `Rule #1 added.`, and `gori run capture --project ci STRAY` started the proxy without a word. They now abort naming the argument and the flag its value belonged to, which on these commands is usually an unquoted multi-word value whose tail words arrived as positionals (`--when host:a AND status:500` reports both `AND` and `status:500`). A word after `--` is refused too. **This is a breaking change** for a script that passed a tolerated extra word: such a call exited 0 before and now exits non-zero. The affected commands are `capture`, `colormarker add|preview|color add`, `decoder list`, `intercept enable|disable`, `issues create`, `oast listen`, `probe rules add`, `project scope add`, `project sandbox on|off`, `rewriter add|preview|bindings|preset list|extract add` and `sitemap tag` (#900)
- TUI: one chord clears a tab, in every tab that can be cleared. gori has four verbs that delete a whole tab's worth of project data — History's flows, Probe's issues, the Authorize queue and the Project ACTIVITY feed — and they answered three different things: History had just gained `⇧X` (#898), Probe and Authorize were reachable only from the space menu under `X`, and Activity had picked up `⇧C`. All four are `⇧X` now, with `X` as the space-menu letter, so the gesture is learned once and carries. `⇧X` rather than `⇧C` because of what sits under the shift: bare `x` is bound in none of the four scopes, while bare `c` is live in all of them — `capture.toggle` globally, and `c` dismisses an issue on the Probe list — and a project wipe one shift below the most-pressed triage key in the app is the arrangement to avoid, not the one to standardise on. Each chord is now named where it can be read before it is pressed: the Help sheet gains a row for Probe, Authorize (which had none at all, though its Shortcuts popup already opened onto the section) and the ACTIVITY pane, and every body hint that can fire the key names it — on Probe and History that means the preview-pane and mode-off states too, not just the default list: the scope does not change with the pane, so naming it in one state and hiding it in the other two would have rebuilt the same gap in miniature. `docs/guide/hotkeys` gains the rule, next to the Ctrl one it sits beside. That was the second half of the objection when Probe's wipe came off a bare letter — a destructive key nothing advertises is the same defect one modifier up, and the menu is not an advertisement you read before you press. In Activity the `Clear filters` menu letter moved from `x` to `N` (`history.mark-clear`'s letter for the same word) so that `x Clear filters` and `X Clear activity` no longer sit one shift apart in one menu with nothing but the shift between resetting a chip and deleting the agent audit trail. All four now ask for confirmation — **Authorize's clear did not before**, and a shared chord has to mean a shared contract, or the tab an operator learns the gesture on is not the tab it costs them something on. It also says what goes, since "the queue" understates the identity results that go with it. Probe's clear answers an empty list with a toast rather than nothing at all, the way Activity's already did. Each verb is still scoped to its own tab; `⇧X` also means "enable/disable everywhere" on the Rewriter and Colormarker rule lists, which have no clear-all verb to collide with (#899)
- Fuzzer: save complete runs permanently, including every request/wire/response byte — the TUI privately spools each run to disk (bounded per run; past that ceiling the archive is declared unavailable and the sweep runs on) while keeping a bounded 5,000-row/64 MiB display window, then `Shift-S` promotes the full archive; run history and automatic bounded restore, `gori run fuzz save/list/show/delete`, and MCP `save_results` with scalar-by-default saved-run list/get/delete tools share the same byte-bounded writer. A spool that stops accepting rows says so at the first rejection rather than at the end of the sweep, and closing a session or a project never waits on a worker for longer than its deadline. Ordinary fuzz runs remain ephemeral. (#897)
- TUI: press `d` in History to delete the selected or marked flows, and `⇧X` to clear the project's History; both still ask for confirmation. `⇧X` has since become the clear-all chord in every tab that has one, Probe, Authorize and the Project ACTIVITY feed included (#898, #899)
- Nix: the flake gains an **overlay**, so a NixOS or home-manager configuration adds `inputs.gori.overlays.default` once and uses `pkgs.gori` anywhere instead of spelling `packages.${system}.default` at each use site. `nix run github:hahwul/gori#gori` now works alongside the bare `nix run`, and `meta.platforms` states the three systems the flake actually builds rather than every platform the compiler runs on (#893)
- Update: `gori update` recognises a **relocated Nix store** — a rootless install keeps one under `~/.local/share/nix/root` and `NIX_STORE_DIR` moves it anywhere. Reading those as an ordinary binary sent the updater off to overwrite an immutable file, so a bare permission error appeared where the "the store is read-only" guidance belongs (#893)
- TUI: Miss Ring gains the four things she could not do, and loses four defects an audit at a realistic run-loop cadence turned up. She now wears a **work badge** while a fuzz, discover, mine or in-flight repeater send is running — driven by the counter the status row's own activity spinner already advances, so the two are in lockstep on a repaint that was already bought, and in `placement: body` (the default) the pane's corner is the only thing on screen saying the run is still going. Her **bar chip carries the mood badge** now (eight cells, the badge borrowed from the same sprite): that placement was leaving "something went wrong" to a hue shift of one gold, which is the signal a washed-out terminal has least of. **Clicking her opens the notification ring** in either placement — she is its face and her bubble is its newest entry, sprite and speech bubble both. And a third motion mode, **`still`**, drops even the blink, so nothing she does on her own moves and she reports no change at all while idle — the doze without the ninety-second wait, for an asciinema recording, a screen reader or a shared tmux pane; reactions still play in all three modes. The defects were all invisible to a green suite: the beat clock ran *after* the state that reads it, so every reaction was stamped one beat early and the `:error` shudder's opening flinch was never drawn at all; a note arriving between two beats reported a frame change for a frame that had not been recomposed, repainting the previous one and delaying its own bubble; the glint sweep's second step landed on a cell the specular gate refused to tint, blanking 400ms out of the middle of it; and in `placement: bar` a quarter of every change she reported moved not one cell of the chip (#894)
- Project: the event feed records **who changed what**, on every surface. Config changes were invisible to it: a scope rule, the sandbox gate, a host override, a `$KEY` var, a Match & Replace rule or the per-project network pins could be edited from the TUI or `gori run` and the log said nothing, so an audit of the project was missing every change a human made. They are now recorded at the MODEL each surface shares — one site per change covers TUI, CLI and MCP, rather than three copies of which the CLI is the one that gets forgotten — and only once the store has confirmed the write, so a refused rule never appears as a control that existed. Values are named (the pattern, both ends of a dial map) except where they are secret by construction: `$KEY` vars are logged by NAME only and an upstream proxy's userinfo is redacted. Every row now carries an **actor** — `tui`, `cli` or `agent` — reusing the three words `flows.source_surface` already stores rather than inventing a second vocabulary; `a` filters by it, MCP `list_events` returns and filters it, and an agent can finally tell its own writes from the operator's (#864)
- Project: an **Activity** pane — a human window over the #124 event feed the AI already tails. `log_agent_action` has recorded every agent mutation and send since #124 so the activity would be *visible to the human*, but the only reader was the MCP `list_events` tool. The sixth Project sub-tab lists that feed newest-first: agent tool calls and their scope-blocked refusals, job results, and the failures that were logged without ever raising a notification (a `$token` extraction that missed, a response that arrived streamed so the rule never ran, a hook that failed and passed the bytes through unchanged). `s` source, `l` level, `a` actor, `/` text, `↵` opens the flow or session an event names, `⇧X` empties the feed behind a confirm. The notification ring is unchanged: it stays the sparse in-memory interrupt channel while this is the durable log, and nothing moves between them. On a busy or oversized feed the pane holds its place: a cursor sitting on the newest row FOLLOWS it, one parked further down keeps its event and the card border reports `↑N new` above it, a burst larger than one page is folded in rather than resetting a list the operator has paged into, and `↓`'s walk back through a feed grown past the 50k scan window survives the liveness commit that lands every few seconds — so "no events match … in the newest N events" can reach the end of the feed and stops being a claim about a window it never left (#864)
- Cookie: a TUI workbench tab for framework signed session cookies (Flask/itsdangerous, Rack, Django) — the JWT tab's sibling, closing #565's last surface-parity gap. The engine, `gori run cookie` and the `cookie_*` MCP tools already did decode/verify/crack/forge; the TUI only decoded (a Decoder-catalog entry), so a human had to drop to the shell to check or forge a cookie. The tab is two lenses over one session, modelled on JWT: **DECODE** — paste or send a cookie into INPUT → DECODED shows the parsed parts → OPTIONS pins the format (`^A`, auto-detects), the Django HMAC algorithm and a signing salt → SECRET holds a candidate key whose verify verdict (`✓`/`✗`) is live, and `c` cracks it over the field read as a wordlist path or a comma-separated candidate list; **FORGE** — edit the session payload (or the opaque Rack value) and re-sign it under a known/cracked secret into a copyable cookie. A Django SESSION cookie signs under a non-default salt, so the salt/algorithm the OPTIONS pane exposes are threaded through the same engine the CLI drives (`Cookie.verify`/`crack` gained the keywords; the three-argument default-salt path is unchanged) — and because a blank field silently used the *generic* `django.core.signing` salt, a real `sessionid` (the tab's headline target) read as `✗ bad key` under the correct secret; the OPTIONS card now shows a ` salt:signing ` badge whenever the format resolves to Django (auto-detected too, not only when pinned) that flips the field to `django.contrib.sessions.backends.signed_cookies` in one click (`Space → Toggle Django salt`), threading the chosen salt through verify, crack **and** forge together. `Space → Send selection to → Cookie` seeds a session from any selected string. Hidden by default (re-enable in settings:tabs), like the other tool tabs (#863)
- TUI: `/` searches the Help tab, Help reference cards, and Hotkeys editor; Enter jumps from Hotkeys results back to the matching binding (#860)
- Proxy: choose a TLS fingerprint **per send**, not only per destination. #822's `outbound_tls` presets answer "always look like Chrome to this origin"; the question they exist for — *does this endpoint answer differently as `chrome` than as `curl`?* — is an A/B on one host, and asking it meant editing a global rule between two sends, which made the two incomparable and changed the handshake for every other tab and background capture hitting that host at the same time. Now a Repeater tab (`␣T`, a chip on the TARGET band, persisted so a reopened tab sends what it sent before) and a fuzz run (`gori run fuzz --tls-preset`, the Fuzzer's **TLS fingerprint** row, MCP `fuzz_start{tls_preset}`) each name their own; `gori run repeater [send|create|h2] --tls-preset` and MCP `send_request{tls_preset}` do the same for one send, and the result reports which handshake produced it. The override **narrows** the destination policy rather than replacing it: it takes the whole ClientHello shape (`preset`/`groups`/`sigalgs`/`ciphers`/`ciphersuites`/`alpn`/`session_tickets`/`ocsp_stapling`) and leaves the client certificate, the protocol range and `permissive` alone — a fingerprint A/B must not quietly become authenticated-vs-anonymous. It resolves through the same `OutboundTlsRule` every dial already resolves, so it enters the SSL-context cache key by construction and two sends with different fingerprints can never share one handshake. `gori settings tls-fingerprint HOST --preset NAME` prints the JA3/JA4 an override actually sends; presets remain approximations, not byte-exact matches. With no override set, behaviour is unchanged (#844)
- Miner: process hooks reach the parameter miner — `gori run mine --hook './sign.sh'` and the MCP `mine_start` `hook` argument pipe each assembled probe through your own command before it ships, so an app that requires every parameter to carry an HMAC, a signed envelope or a per-request nonce can be mined at all (a raw candidate it rejects before reacting teaches the miner nothing). The command is the same no-shell `ProcessHook` primitive the Rewriter/Decoder/Probe seams use; a hook that cannot run **skips that probe with a reason** instead of sending an unsigned request the miner would then read as a clean negative, and the run continues rather than wedging. The timeout is the one shared `hooks.timeout_secs` budget **per outbound request** — a mine's request count is already bounded by `--max-requests` and its bucket/bisection/confirm tree, so the total hook cost is bounded with it. The TUI config overlay leaves it (and `--wordlist`) at defaults, the same field-free parity gap. The Fuzzer's run-wide `--encode`/processors catalog stays pure-transform on purpose: an exec belongs on the per-position `§value¦exec:…§` chain, which already carries the failure channel and up-front argv validation a closed processor catalog cannot (#846)
- Hooks: finish the "a hook runs when you SEND, not when you look" contract inside the fuzz engine (#851 follow-ups). A template with one hook marker and one failing pure marker now refuses **before** the hook fires — pure chains are ordered first, so a refusal decidable without a side effect is decided without one. `Generator#baseline_raw` is memoized, so `Plan.build`'s three structural questions fork an `exec:` chain once, not three times. The Repeater send path and `Fuzz::Plan` now share one chain validator (it lives in `fuzz/`, the TUI calls it) and one refusal sentence, and `WsScript#apply_chains` forwards `run_hooks` like the other composites (#852)
- Settings profiles: a shareable profile can carry commands, and both ends now say so. Five sections can hold one — `rewriter` rules with `op: pipe`, `scan_rules` with `kind: exec`, a `decoder` chain's `exec:` step, `statusline.command` (a `/bin/sh -c` on a timer) and `editor.command` — and they still export like any other setting, because a team standardising on one re-signing hook is what hooks are for. But `gori settings export` now reports how many it wrote on stderr, and `gori settings import` lists each one with its name, mechanism and command before writing anything, then **refuses until you pass `--allow-commands`** (no prompt, so a scripted import stays scriptable). `--dry-run` prints the same list; narrowing with `--sections` narrows it too, so an import that arms nothing asks for nothing. `gori settings sections` marks the five, and the listing escapes anything in a name or command that could make the line read as something other than what runs (#842)
- Settings profiles: `hooks.timeout_secs` can travel in a profile. It was serialized into every export but missing from the section registry, so importing one warned `unrecognised section(s) ignored: hooks` and dropped it — a profile carrying `pipe` rules silently lost the timeout that bounds them (#842)
- Retest: a diff row reaches Issues and Notes — `⇧F` files the selected endpoint as an Issue prefilled with both projects, both sides' answers and what moved, `n` records the same as a Note in one keystroke, and the capture on the open project's side is linked as evidence rather than copied. A coverage-gap row files as `info` and says the newer capture never requested the endpoint, so it cannot claim a removal the diff never observed; `gori run diff --format json` carries that same sentence on every row (#845)
- Issues: score a finding with CVSS and let the severity follow. `Space` → **Set CVSS** (or `↵` on the issue form's `cvss` row) opens a calculator you can either type a vector into or build one in — CVSS **v3.1 or v4.0**, each version keeping its own selections; headless it is `gori run issues --cvss` and the MCP `cvss` argument, all of which refuse a string they cannot score. Adds `cvss:>=7` filtering, CVSS-aware Markdown/JSON exports, and real CVSS scores in SARIF's `security-severity` (#575)
- Rewriter: response-modification presets — one keystroke installs the Burp-style options every tester writes by hand (unhide hidden form fields, enable disabled/readonly controls, remove `maxlength`, strip client-side validation, drop CSP, drop security headers, disable SRI). A preset installs ordinary, visible, editable, disable-able Match & Replace rules through the same write path as manual authoring — nothing hidden, nothing magic — so they can be edited or deleted like any other rule. `p` on the Rewriter tab, `gori run rewriter preset list|add <name>`, and the MCP `list_rule_presets` / `create_rule_from_preset` tools all install from the one static catalog; the regex rules stay conservative about markup and never run a command (#821)
- External process hooks: pipe bytes through your own command instead of a plugin API — a Rewriter `pipe` op, a Decoder chain `exec:` step, and a Probe `exec` rule, all one primitive. The command is exec'd directly with no shell and runs with YOUR privileges (unsandboxed, the same trust level as any Rewriter rule); every run has a hard timeout (`hooks.timeout_secs`, default 5s) and a 32 MiB stdout cap, and on timeout, non-zero exit, failed spawn or oversized output the original bytes pass through unchanged with a notice — the proxy never stalls on a hook. The MCP `decode` tool refuses `exec:` steps: it stays pure compute. A hook runs when you SEND, not when you look: the surfaces that replay a `§value¦chain§` to DRAW one — the `^Q` chain editor's preview, a rebuilt Fuzzer result row, the Repeater's Content-Length reflection — withhold the `exec:` step and say so, and reopening a project restores every saved Repeater tab and Decoder sub-tab without firing their commands. The Decoder tab's own chain stays live by design (#818)
- Retest: diff two projects at endpoint scale — what is new, gone, or answering differently since the last engagement. A Diff sub-tab under Target, `gori run diff` (text/json/md), and the MCP `diff_projects` tool; endpoints are keyed by the Sitemap's folded template, and an endpoint the newer capture never requested is reported as a coverage gap rather than as removed (#824)
- Proxy: per-destination outbound TLS fingerprint control — `groups`, `sigalgs`, TLS 1.3 `ciphersuites` and an ordered `alpn` list on `outbound_tls`, plus `chrome`/`firefox`/`safari`/`curl` presets, so an origin's anti-bot layer sees a browser-shaped ClientHello instead of gori's OpenSSL one. `gori settings tls-fingerprint` prints the JA3/JA4 that actually goes out; presets are approximations, not byte-exact matches (#822)
- gRPC: point a project at a `protoc` descriptor set (`Project → Project settings → Proto schema`, or drop one in `~/.gori/protos/`) and captured protobuf renders as named, typed fields — the request path `/package.Service/Method` is the binding. Undeclared field numbers and wire/schema disagreements are still shown, the raw wire tree stays beside the lens on every surface, and with no descriptor set loaded nothing changes (#823)
- Fuzzer: sweep a schema-known gRPC field instead of the octets of a varint. `gori run fuzz --field role`, the MCP `fields` argument and the Fuzzer tab's **gRPC field(s)** row name a DECLARATION — a field name, a path into a nested message (`profile.age`), a field number, `tags[1]` for one occurrence of a repeated field — and each payload goes through that declaration on its way to bytes, because `-3` is a different set of octets as `int32`, `sint32`, `bool` or an enum. Every byte outside the fuzzed field is copied from the capture and the 5-byte gRPC length prefix follows the message it now describes. A field the schema does not declare, one whose wire type the declaration contradicts, and a payload the declared type cannot hold are all refused before the first request (#843)
- gRPC: ask the target for its own schema — `grpc.reflection.v1` / `v1alpha` server reflection as a second schema source, so a server that answers reflection needs no `protoc --descriptor_set_out` step. `gori run grpc reflect <url>`, the `grpc_reflect` MCP tool, and a verb are the only callers: nothing on the capture, flow-open or project-open path fetches, and the result is cached per `scheme://authority` so one operator-initiated fetch serves every later flow on that target across restarts (`grpc forget` drops it). The fetch goes through the outbound scope gate before the dialer like any other active request, and both reflection paths are checked, not just the first (#827)
- TUI: syntax-highlight HTTP structure and structured bodies in the History Request/Response preview, and decode compressed request and response bodies there and in Fuzzer result details — the preview now reads both halves through the same entity seam the History detail pane always has, a binary body gets the detail pane's `— binary body (N) —` placeholder instead of raw bytes rendered as text, and a flow that re-fetches every frame (pending, 101, h2) no longer re-inflates its body behind an unchanged pane (#834)
- Proxy: with `network.strip_alt_svc` off, a response advertising `Alt-Svc: h3=…` now says so on the flow instead of passing silently — the advertisement still reaches the client byte-for-byte, but a client leaving for QUIC is a reported blind spot rather than an unexplained gap in History (#835)
- TUI: the project picker marks a project an MCP client is bound to, and an open project shows a clickable `mcp:<client>` top-bar chip that lists the attached agents (#815)
- TUI: the Comparer flow picker follows the active project Scope lens, and says `no flows in scope` rather than `no flows captured yet` when the lens hides everything; turning the lens off still exposes every captured flow (#809)
- Proxy: distinguish SOCKS5 local DNS from SOCKS5H proxy DNS (existing SOCKS5 rules that need remote resolution should switch to SOCKS5H), add project-scoped proxy authentication and destination filtering, reach an upstream HTTP CONNECT proxy over TLS with the new `http+tls://` scheme and `http+tls` rule kind, and route updater/OAST traffic through the same fail-closed upstream path. Project proxy passwords are shown only while editing and stored in the owner-only project database. `https://` still means the plaintext proxy it has always meant and is now flagged at startup rather than reinterpreted; the TLS hop writes `CONNECT` and `Proxy-Authorization` inside the session and is verified on the proxy's own hostname under `network.upstream_proxy_ca` / `network.upstream_proxy_insecure`, which the origin's `--insecure-upstream` does not touch (#434, #3)

## v0.4.0

### New features

- Authorize: replay a captured request under saved identities against a baseline, on all three surfaces. Passive replay of what the browser touches, `gori run authorize`, and the MCP tools share one plan (#707, #710)
- History views: `v` picks a named filter that stays on — seven built-ins, project/global saved views, `gori run views` / `--view`, and MCP `*_view`. A project opens on `History + Repeater` (#776)
- History provenance: a SRC column and `src:` name who sent each flow. TUI Repeater sends record by default; Authorize and Probe ignore gori-originated traffic on the unattended path (#770)
- Proxy: a `socks5` inbound listener, and `network.strip_alt_svc` so a browser cannot leave for HTTP/3 (#786)
- Fuzzer: WebSocket session sweep, last-byte-sync race over HTTP/1.1, and URL-encoding of query/form payloads by default (#705, #795)
- Import: WSDL 1.1 becomes one SOAP template per operation (#794)
- Decoder: brotli, zstd, MessagePack and CBOR; MessagePack/CBOR bodies render as JSON in the detail pane (#786)
- Session: build a slot from a captured login (`session from-flow` / `create_session_slot{flow_id}`) (#719)
- Sitemap: query-string variants fold into one path node (`⇧G` restores the literals) (#750)
- Probe: shared insertion points, plus COOP, CSP `base-uri`, JWT key-injection, broad-domain cookie, MIME-confusion and error-based SQLi (#788, #793)
- Export: HAR round-trips WebSocket messages; Issues export SARIF (#719, #792)
- JWT: `--payload`/`--set` on the CLI, MCP set parity, tab visible by default (#747)
- QL: `scope:` makes the in-scope lens a query term (#754)
- MCP: `--install-hermes` writes `~/.hermes/config.yaml` (#785)

### Changes

- TUI: empty-tab cards; a ⌕ sub-tab picker; Help as palette cards; clickable filter chips; `Space ⇧B` opens a decoded body in the desktop opener; `^G`/`^F` find in Decoder and Fuzzer; factory reset; `y` with no selection copies the pane (#691–#704, #759, #782, #796, #801)
- CLI: hide empty projects by default; `history show --format curl`; json/jsonl rows carry `url` and `headers`; `history delete -q`; leftover verbs after `--project` no longer silently list or scan (#722, #750)
- Store: a capturing TUI no longer shares SQLite's writer with a read-only peer; live Match&Replace, extract rules and probe mode pick up a peer's edit on the tick (#752)
- Miner: refuse a phantom baseline, skip names the request already carries, retry calibration
- MCP: an unreadable integer is refused by name before the work; `rate` is fractional (#724)
- Fixes: Comparer phantom diffs; Decoder OUTPUT folding newlines; JWT weak-secret re-signs under the token's HMAC alg; host overrides reach Authorize and Probe; OAST release/poll honesty; Colormarker refused writes; Match & Replace obs-fold; HTTP/2 intercept that cannot re-encode; 33 engine defects from a source audit; TLS / WS-over-h2 / compressed-rewrite protocol gaps (#729, #741, #751, #802–#807)

## v0.3.2

A hotfix release: the bug fixes written since v0.3.1, cherry-picked onto it. The features on the way to the next minor are not in it.

- Open browser: a browser that never starts is reported as such, with its own error and how it died, instead of `opened` — gori only checked that the process spawned, not that it survived, and closed the browser's stderr, which was the one thing that could have explained the failure. The verdict comes from waiting on the child rather than a deadline, so it holds on a machine slow enough to lose that race. Brave is also launched without `--test-type`, which 1.92+ treats as a unit-test signal and aborts on — Chrome still gets it, to hide the SPKI-pin infobar (#700, #716, #721)
- Scope: a regex EXCLUDE rule no longer fails **open** on a target that is not valid UTF-8 — it scrubbed nothing and `rescue false` read as "does not match", which is scope evasion. The History/Sitemap SQL lens also now agrees with the live gate on bracketed IPv6 hosts, brace globs and non-ASCII case (#688, #699)
- Project settings: a host override reaches gori's own reserved name from either layer and folds case and a trailing root dot into one key, and the TUI refreshes the project env table before writing it back over a peer's edit (#687, #689)
- TUI: the statusline gets its own timeout and reports why it is blank, instead of killing the script that was about to answer (#690)
- Stability: a crash audit across the CLI, TUI, store and MCP — a non-UTF-8 byte no longer aborts a command through PCRE2, a stale read cursor no longer takes the session down, and a poisoned release tag no longer crashes every later launch from cache. An MCP discover job also flushed its findings after going terminal, so a run finishing during a `switch_project` could write them into the project you had just moved to (#699)
- MCP: `gori mcp --install-claude` now writes Claude Desktop's config where the running platform actually keeps it — `$XDG_CONFIG_HOME/Claude/` (default `~/.config/Claude/`) on Linux, not a macOS `~/Library/Application Support/…` path built under a Linux `$HOME` (#718)

## v0.3.1

- Filters: one query grammar on every filter surface, content terms included; `header:`/`body:` now take a side, and the filter bar teaches its own syntax (#668, #674)
- Decoder: recognize GraphQL/gRPC bodies by media-type essence, decode HTML entities, and cover WS subscriptions and both-side protobuf (#663)
- Probe: scan binary WebSocket frames for credential shapes, stop DOM-XSS pairing on shapes that carry no taint, and fix two rule gates that failed silently (#671, #672, #676)
- TUI: a copy key in INS mode (`y` there types a `y`), a wrap-lines toggle with horizontal scrolling back, sandbox togglable from the palette, and a deeper brand gold in the light theme (#652, #657, #670, #677, #680)
- Performance: chunked imports, a fast path for parked TLS sockets, cheaper discover fingerprinting, n-ary miner bisection, probe findings in one writer round-trip, and probe lists paged in SQL (#655, #665, #666, #667, #669, #673, #675)
- Stability: ~40 ways gori could crash, hang, overrun a stop or lose a run's results; an MCP server that survives the three things that killed it; project delete/`--db`/durable-write and intercept ack fixes (#651, #654, #658, #659, #678, #679)
- Docs: a Playbooks section that teaches gori's workflows by doing them, AUTOMATION split into Run and MCP with a new Scripting guide, and a refreshed landing hero (#650, #664, #682)

## v0.3.0

### New features

- Colormarker: a new tab for row-colour rules in History — global and project scope, custom colours defined, recoloured and renamed in place, reorder, painted consistently across TUI/CLI/MCP (#632, #640)
- Cookie workbench: parse, verify, brute-force, and forge Flask, Rack, and Django session cookies (#569)
- HTTP/2, first-class: hold, edit and drop individual streams at intercept, a complete HPACK encoder, Match&Replace on h2 heads, a per-stream sandbox, and a field-native send path for the shapes h1 head text cannot express (#510, #512, #513, #515, #649)
- WebSocket: Match & Replace over messages, and hold/edit/drop at intercept with `proto:ws` (#500, #533, #537)
- Session bindings: extract a value from proxy traffic once, resolve `$NAME` from it at send time (#501, #530, #535)
- Proxy: reverse-proxy listener mode, transparent listeners that read the kernel's original destination, and a short-circuit rule op that answers a request without dialing upstream (#509, #520, #528)
- Decoder: schema-less protobuf/gRPC wire decoder, new encodings (quoted-printable, punycode, base36/62, xml/shell/c escapes, homoglyph/typo), saved chains callable by name, and a named library with a picker (#505, #558, #567)
- Fuzzer: built-in payload preset sets (SQLi and more), selectable across TUI/CLI/MCP (#568)
- OAST: blind SSRF — plant a payload, promote the finding when the target calls home (#609)
- Export: HAR export, with the import-side fields to round-trip it (#506)
- Miss Ring: an opt-in companion in the body's corner, and on the project picker delivering the update notice; on `lively` she now also plays one of four idle gestures — a yawn, a smile, a squint or a deadpan — about once a minute, in the status-bar chip as well as the body sprite (#474, #548, #550)

### Changes

- Discover: read the target's well-known documents (OIDC/OAuth discovery, `security.txt`, sitemaps, …) and extract endpoints from JS bundles, JSON, source maps and inline `<script>`; keep each finding's request/response and open them from the findings table; hold and re-measure a drifting soft-404 baseline instead of reporting hundreds of limiter hits; report a real page on a wildcard-200 origin; bound per-response spend and stop a JS literal from buying a brute-force sweep (#605, #638)
- Comparer: per-row change highlighting with `n`/`⇧N` navigation and `f` fold, per-column `status·size·time` headers with the A→B delta, and **Send to Comparer** from the Repeater, Sitemap and Fuzzer rows; `--context=N` fold parity in CLI/MCP
- Rewriter: rules scoped global or project (replacing the s/o preset library), shown on the tab bar by default right of Comparer (#544, #611)
- Probe: passive-rule improvements, the OAST SSRF rule classed CWE-918, and a navigable AFFECTED URLS list in a finding
- Miner: latency-bound scheduling — one work queue for all locations, parallel calibration (~2.4x)
- JWT: the lens switch (`^T`) now shows on the pane it acts on; new `dancheong` (dark) and `hanji` (light) themes
- Project picker: multi-select over the project list — `Tab` / `⇧Tab` mark and step, `⇧↑` / `⇧↓` extend a range, `ctrl-a` marks everything the search shows, `esc` clears. The space menu's **Delete** then acts on the marks if any are set, else the cursor row; it names what it is about to wipe, says how much of the set the current search is hiding, and keeps (rather than silently skips) a project another gori still has open
- `gori ca`: reject a flag written before the verb, repair a CA directory missing one of the key/cert pair, and reject an Ed25519/Ed448 or mismatched-key root with an operator-legible message instead of failing at the first CONNECT
- Fixes: bracketed-paste freeze and poison in the Repeater; the `--` separator dropping subcommand args across ~60 `gori run` sites; colormarker custom-colour persistence and reorder writes; clipboard OSC 52 over tty; project-settings and host-override reload/rollback audit; OAST partial-poll evidence loss; and many dogfood-surfaced bugs

## v0.2.0

- Proxy: upstream connection rules with per-host routing, SOCKS5 and proxy auth; a TLS pass-through list that is never MITM'd; per-destination outbound TLS (client certificates, protocol floor, ciphers); a setting to force HTTP/1.1; transparent listeners and additional listeners alongside the primary bind (#434, #435, #436, #437, #438)
- Proxy: harden the HTTP/2 assembler against CONTINUATION spoofing and stream-slot exhaustion, re-sync framing after a head rewrite so Match&Replace can't smuggle, and reject bare-CR header obfuscation and ambiguous response framing (#341, #403, #409, #412, #417)
- Proxy: serve the CA-download page at a reserved host, `gori.proxy` (#347)
- TUI: multi-select in History, the Intercept queue, the Sitemap tree, and the Issues list, so the space menu acts on N items at once (#442, #459, #460, #461)
- TUI: the Project tab becomes sub-tabs instead of five tiled panes, Network settings gain upstream-rules and outbound-TLS tables, a Keys section picks the command modifier (⌥ reaches the shortcuts Ctrl can't), plus `rosepine` and `tokyonight_day` themes (#440, #454, #458, #462, #463)
- TUI: export the current note to Markdown from the Notes space menu, and ask where to write the Issues report instead of always overwriting `<project dir>/issues.{md,json}`. Export is `⇧E` on both tabs; the Issues list's old `x` is freed, so `x` now means "Select line" everywhere (#432)
- Settings: `--config PATH` plus settings export/import profiles, per-project connect/idle timeouts and capture limit, and a unified retention policy (#439, #440, #441, #448, #450, #455)
- Import: read Postman collections, Insomnia exports, and Burp XML (#453)
- Probe: active-scan rules for open redirect, CRLF/response-header injection, host-header injection, access-control bypass, NGINX-style parameter traversal, GraphQL introspection, SSTI, and Next.js server-action missing authorization; passive rules for JWT weaknesses, source maps, SRI, and directory listing; a manual unsafe-method opt-in and AGGRESSIVE mode (#299, #342, #343, #346, #349, #350, #451)
- CLI/MCP: bring `gori run` and `gori mcp` to TUI parity, and create/delete projects from `gori run project` (#351, #352)
- Performance: move trigram FTS indexing off the capture commit path, and reuse one HTTP/1.1 connection across a fuzz sweep (up to 20x on HTTPS) (#428, #433)
- Security: close request-splicing and scope-gate holes across Discover, Fuzzer, Repeater and Scope — crawled-link splicing, unvalidated redirect `Location`, per-URL probe authorization, fail-open scope, irregular request-line whitespace, and `wss://` targets dialing cleartext (#390-#397, #404-#407, #418-#422)
- Security: keep gori's own files owner-only — the CA private key is now 0600 from creation (it used to land at the umask default and get chmod'd a syscall later, and a key that got loose any other way was never re-tightened) and is re-asserted on every load, and a settings export carrying a secret is written 0600. `--config` and `--ca-dir` no longer re-mode a directory the operator merely named (#466, #467)
- Say what went wrong instead of swallowing it: a TUI session that can't open (a bad `--db`, an unreadable store) reports why on the project picker rather than dropping the operator on "no projects yet"; an unparseable `settings.json` says it is falling back to defaults rather than resetting the bind, upstream rules and TLS pass-through list in silence; and a path that should be a directory but isn't (`--ca-dir notes.txt`) is named as such instead of surfacing as `BIO_new_file(...) failed` or a raw backtrace
- Refactor: a single outbound chokepoint for the active-traffic scope gate, one Plan builder per engine (fuzz, discover, miner, repeater, sequencer) shared by TUI/CLI/MCP, and all 28 TUI modals on one Overlay seam (#354, #355, #356, #361)
- Packaging and docs: Nix flake with an update channel, `AGENTS.md`, `DESIGN.md` with the P0-P8 principles, and an install script that survives GitHub API rate limits (#338, #345, #353, #360, #429)

## v0.1.4

- Proxy: fix HTTPS blank pages / empty History — reflect origin ALPN so h1-only origins load, resolve the system CA trust store for upstream verification, and report TLS-verify failures separately from connect failures (#332, #333, #334, #336)
- Proxy: stop an upstream RST leaving a flow stuck Pending forever (#330)
- Scope-gate every outbound path so Sandbox mode holds: Repeater, Fuzzer, Miner, Sequencer (CLI and TUI), with `--allow-unscoped` opt-out (#322, #330, #339)
- Import: reject CR/LF/NUL smuggling in HAR/OpenAPI, and neutralize control bytes in decoder/JWT text output (#322, #324, #339)
- CLI/MCP parity: add Comparer (`gori run compare` / `compare_flows`), CLI Intercept, CLI WS repeater send, MCP scope/env/host-override mutation and `import_flows`, `gori run probe --active` (#321, #326)
- MCP: fix a credential leak in `get_repeater_context`, cap unbounded h1 capture reads, and surface `PROJECT_BUSY` on rolled-back writes (#335)
- TUI: Repeater `^N` mirrors the target host into the Host header, Fuzzer wordlist field suggests recent and favorited paths, tutorial navigation fixes (#314, #315, #335)
- OAST: support global-scope providers alongside project scope (#313)
- Fix dogfooding-surfaced bugs across QL (`url:`, size and `dur:` units, uppercase schemes), Discover, Sequencer, Repeater, browser CA trust warning, `settings.json` formatting, and multipart form data (#312, #316, #317, #318, #319, #325, #337)

## v0.1.3

- Fix 30 confirmed bugs found across three build-and-dogfood passes: TUI (`--db`, Repeater NUL-truncated bodies, Rewriter hot-reload, Sequencer/Miner/OAST, Scope reload, log redirection), CLI (`oast listen --help` crash, Issues/Sitemap export encoding), proxy (WS close-handshake race, h2 preface on intercept), MCP, Import (HAR/OpenAPI/URL-list CRLF injection), Fuzzer auto-calibration, and more (#301, #307, #310)
- CLI: accept `-V` as a version flag alias (#298)
- TUI: match banner and wordmark gold to the real logo (#308)
- Docs: dynamic landing page, nav/sidebar reorganization, logo download menu, homepage title (#300, #302, #303, #304, #305, #306, #309)

## v0.1.2

- MCP: start **unbound** outside a Git workspace so `gori mcp --install-*` always connects; agents bind via `list_projects` / `create_project` / `switch_project`. Traffic tools return `NO_PROJECT` until bound, and `--no-project` forces unbound inside a workspace (#295)
- TUI: show a startup update-available notice on the project picker (#293)
- TUI: make the NOR/INS editor mode badge more discoverable with click-to-toggle (#294)
- TUI: fix clickable OAST callbacks, pane navigation, and Rewriter preview (#296)
- Tests: expand spec coverage across pure and harness-testable modules (#297)

## v0.1.1

- Fix wide-character/emoji rendering and caret placement in the TUI editors with a per-grapheme width model (#281, #285, #289, #291)
- Fix proxy self-loop guards under wildcard binds, serve the CA cert page to LAN clients, and show a dialable bind address (#279, #284, #287)
- Stop background reconcile from resetting the caret in Repeater and Notes (#277, #286)
- Add Snap packaging and publish workflow (#276)
- Docs: install command picker, sidebar regrouping, AI setup guide, landing refresh (#275, #282, #283, #288, #290)

## v0.1.0

First Release
